## 1. Task Expansion

- [x] 1.1 为 `TdxTaskManager` 增加 `watchlist_overview`，复用批量总览能力。
- [x] 1.2 为 `TdxTaskManager` 增加 `sector_formula_scan`，复用板块成分与公式扫描能力。

## 2. CLI Expansion

- [x] 2.1 为 `task` 二级命令增加 `watchlist-overview`。
- [x] 2.2 为 `task` 二级命令增加 `sector-formula-scan`。

## 3. Profiles And Docs

- [x] 3.1 为新 task 增加默认 profile。
- [x] 3.2 更新 task 层使用文档与示例。

## 4. Verification

- [x] 4.1 补充 task manager 与 task CLI 测试。
- [x] 4.2 运行定向测试验证新增 task 不影响现有 task/manager 入口。
