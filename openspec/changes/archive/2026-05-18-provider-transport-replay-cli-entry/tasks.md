# Tasks

## 1. Contract

- [x] Add OpenSpec deltas for provider replay CLI entry points.

## 2. Tests

- [x] Add failing parser tests for `provider-replay serve` and `config-check`.
- [x] Add failing handler tests for config-check and serve delegation.

## 3. Implementation

- [x] Add CLI parser and handler for provider replay commands.
- [x] Add runtime example config.

## 4. Registry / Verification

- [x] Update `FUNCTION_TREE.md` E-05 with status, evidence, and boundary.
- [x] Run focused tests.
- [x] Run OpenSpec validation.
- [x] Run `git diff --check`.
