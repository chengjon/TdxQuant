## Why

`D:\MyCode3\tdx` contains earlier TdxQuant functionality documentation, real-machine Win32/UIA/HID validation evidence, and historical implementation context, while this repository has since moved forward with additional bridge, replay, registry, task, and trade governance capabilities. A controlled merge is needed so useful `tdx` functionality and validation conclusions are adopted without overwriting newer `TdxQuant` behavior or weakening the single feature-truth model.

## What Changes

- Reconcile `D:\MyCode3\tdx` README, function map, and PingAn/TongDaXin Win32 validation summary against the current repository.
- Treat `FUNCTION_TREE.md` as the single destination for capability status, evidence, and boundary updates.
- Preserve current repository implementations when they are newer than the `tdx` copy, especially bridge, block sync, provider replay, subscription background control, function registry validation, and extended trade governance.
- Adopt the PingAn real-machine mixed UIA/HID/Win32 buy-loop conclusion as formal evidence for the existing desktop trading capability.
- Register the TongDaXin trading line as probe/diagnostic-only unless its trade business layer accepts automated security-code input; do not advertise full TongDaXin live order submission.
- Import only curated validation artifacts and documentation references; exclude caches, generated bytecode, giant raw probe dumps, and account-sensitive evidence unless explicitly scrubbed.

## Capabilities

### New Capabilities
- `tdx-functional-surface-merge`: Defines the controlled reconciliation workflow for adopting external `tdx` project functionality, documentation, and validation evidence into the current repository without source regressions.

### Modified Capabilities
- `tdx-function-tree-registry`: The registry must capture external merge evidence and remain the only authoritative feature-status surface after reconciliation.
- `tdx-desktop-trading-management`: Desktop trading status must distinguish PingAn mixed-chain validated execution from TongDaXin probe-only trading boundaries.
- `tdx-trading-hid-bridge`: TongDaXin HID bridge status must remain scoped to prerequisite probing and HID-assisted buy probing until the business-layer security-code acceptance blocker is resolved.

## Impact

- Documentation and registry files: `README.md`, `FUNCTION_TREE.md`, `docs/TdxQuant_Project_Function_Map.md`, and selected docs under `docs/`.
- OpenSpec specs for feature registry, desktop trading management, and TongDaXin HID bridge boundaries.
- Optional curated runtime evidence under `runtime/verification/` or linked documentation indexes.
- Focused tests around feature registry validation and existing trade/bridge behavior; no direct dependency or package upgrade is expected.
- No breaking API change is intended; this change is primarily controlled adoption, documentation, boundary registration, and regression protection.
