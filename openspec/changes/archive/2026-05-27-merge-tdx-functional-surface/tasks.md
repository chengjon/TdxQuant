## 1. Merge Inventory

- [x] 1.1 Review the requested external documents: `D:\MyCode3\tdx\README.md`, `D:\MyCode3\tdx\docs\TdxQuant_Project_Function_Map.md`, and `D:\MyCode3\tdx\docs\pingan-tdx-win32-validation-summary.md`.
- [x] 1.2 Build a concise merge matrix that classifies external `tdx` material as current-newer, external-only useful evidence, conflicting status claim, generated/noisy artifact, or sensitive artifact.
- [x] 1.3 Identify any external source/test files that contain useful logic not already present in the current repository, and explicitly record when no code import is needed.

## 2. Registry And Documentation Reconciliation

- [x] 2.1 Update `FUNCTION_TREE.md` so accepted external `tdx` conclusions have explicit status, evidence, and boundary rows or notes.
- [x] 2.2 Update `README.md` to align the branch positioning with the reconciled PingAn and TongDaXin trading boundaries.
- [x] 2.3 Update `docs/TdxQuant_Project_Function_Map.md` only as background documentation, preserving `FUNCTION_TREE.md` as the authoritative status registry.
- [x] 2.4 Add or update a documentation note for the external merge matrix and evidence selection rationale.

## 3. Trading Boundary Adoption

- [x] 3.1 Register PingAn desktop buy execution as a validated mixed UIA/HID/Win32 chain rather than pure Win32 or pure UIA automation.
- [x] 3.2 Register TongDaXin trading as probe/diagnostic scoped until business-layer security-code input acceptance is evidenced.
- [x] 3.3 Confirm existing `TdxTradeManager`, `TradeService`, submission ledger, and trade audit documentation remain compatible with the adopted PingAn conclusion.

## 4. Evidence Curation

- [x] 4.1 Select a minimal set of PingAn and TongDaXin validation references or sanitized artifacts from `D:\MyCode3\tdx`.
- [x] 4.2 Import selected artifacts into a stable current-repository location only if they are reviewable and safe to check in.
- [x] 4.3 Document any external evidence files intentionally left outside the repository because they are too large, noisy, generated, or sensitive.

## 5. Verification

- [x] 5.1 Run the FUNCTION_TREE registry validator after registry updates.
- [x] 5.2 Run focused desktop trade tests that do not require a live client, including relevant PingAn gateway/manager/CLI tests.
- [x] 5.3 Run focused bridge/HID tests covering current TongDaXin probe boundaries where available.
- [x] 5.4 Run `python -m pytest` for the full suite if local runtime conditions allow, or document why full-suite verification was deferred.

## 6. Change Closure

- [x] 6.1 Run `openspec validate merge-tdx-functional-surface --strict`.
- [x] 6.2 Review `git status --short` and confirm the final diff excludes generated bytecode, pytest caches, giant raw dumps, and unrelated dirty-worktree changes.
- [x] 6.3 Summarize the adopted functionality, rejected/deferred claims, verification results, and remaining risks for handoff.

## Closure Notes

- Merge-owned status is limited to `FUNCTION_TREE.md`, `README.md`, `docs/TdxQuant_Project_Function_Map.md`, `docs/TdxQuant_tdx_functional_surface_merge.md`, and `openspec/changes/merge-tdx-functional-surface/`.
- The merge-owned files are docs/spec artifacts only; no generated bytecode, pytest cache, giant raw dump, or external account-sensitive JSON artifact was imported.
- Full-worktree `git status --short --untracked-files=all` still shows unrelated pre-existing dirty changes in other docs, API/CLI/test files, `docs/web_docs/`, `openspec/changes/subscription-governance-sample-summary/`, and `tdxquant/api/trade_api.py`. Those are not part of this change and should not be staged with this merge.
- Handoff summary: adopt PingAn as validated mixed UIA/HID/Win32 desktop execution; keep TongDaXin trading as probe/diagnostic-only until security-code business-layer acceptance is evidenced; preserve current repository code because it is newer than the external `tdx` copy; retain external raw evidence by reference instead of importing it.
