## 1. Contract

- [x] 1.1 Add OpenSpec proposal, design, and delta specs for CLI/HTTP runtime identity summary views.
- [x] 1.2 Validate the active OpenSpec change before implementation.

## 2. Tests

- [x] 2.1 Add CLI summary coverage for runtime identity projection without raw `control`/`watch_status`.
- [x] 2.2 Add HTTP summary coverage for runtime identity projection without raw `control`/`watch_status`.

## 3. Implementation

- [x] 3.1 Add selected runtime identity projection to bridge watch-status CLI summary output.
- [x] 3.2 Add selected runtime identity projection to worker bridge HTTP summary output.

## 4. Registry and Verification

- [x] 4.1 Update `FUNCTION_TREE.md` B-16/E-09 evidence and boundaries.
- [x] 4.2 Run focused tests, OpenSpec validation, function-tree validation, and whitespace checks.
- [x] 4.3 Archive the OpenSpec change and re-run verification before committing.
