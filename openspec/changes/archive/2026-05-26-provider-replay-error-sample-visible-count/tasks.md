# Tasks

## 1. Contract

- [x] 1.1 Add OpenSpec proposal, design, and provider transport replay service delta.
- [x] 1.2 Validate the active OpenSpec change before implementation.

## 2. Tests

- [x] 2.1 Add probe summary test coverage for `error_sample_visible_count` when no samples are visible.
- [x] 2.2 Add probe summary test coverage for `error_sample_visible_count` when sample candidates are truncated.

## 3. Implementation

- [x] 3.1 Add read-only visible sample count without changing probe execution, sample limit, or lifecycle behavior.

## 4. Registry and Verification

- [x] 4.1 Update `FUNCTION_TREE.md` E-06 evidence and boundary without claiming daemon lifecycle control.
- [x] 4.2 Run focused tests, OpenSpec validation, diff checks, and the FUNCTION_TREE validator before archive and commit.
