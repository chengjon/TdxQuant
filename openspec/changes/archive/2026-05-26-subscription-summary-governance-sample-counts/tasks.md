# Tasks

## 1. Contract

- [x] 1.1 Add OpenSpec proposal, design, and subscription summary delta for governance sample counts.
- [x] 1.2 Validate the active OpenSpec change before implementation.

## 2. Tests

- [x] 2.1 Add HTTP watch-status summary assertions for `governance.reason_sample_count` and `governance.action_sample_count`.
- [x] 2.2 Add CLI watch-status summary assertions for `governance.reason_sample_count` and `governance.action_sample_count`.

## 3. Implementation

- [x] 3.1 Derive `governance.reason_sample_count` in HTTP and CLI summary views.
- [x] 3.2 Derive `governance.action_sample_count` in HTTP and CLI summary views.

## 4. Registry and Verification

- [x] 4.1 Update `FUNCTION_TREE.md` E-09/B-16 evidence and boundary without claiming lifecycle control.
- [x] 4.2 Run focused tests, OpenSpec validation, diff checks, and the FUNCTION_TREE validator before archive and commit.
