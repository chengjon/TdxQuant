# Tasks

## 1. Contract

- [x] 1.1 Add OpenSpec proposal, design, and subscription summary delta for evaluation status key counts.
- [x] 1.2 Validate the active OpenSpec change before implementation.

## 2. Tests

- [x] 2.1 Add subscription watch status summary assertions for `component_status_key_count`.
- [x] 2.2 Add subscription watch status summary assertions for `evaluated_status_key_count`.
- [x] 2.3 Update HTTP/CLI exact expected payloads if existing summary projection tests require the additive fields.

## 3. Implementation

- [x] 3.1 Derive both key-count fields in `status_summary.governance.evaluation_summary`.

## 4. Registry and Verification

- [x] 4.1 Update `FUNCTION_TREE.md` B-16/E-09 evidence and boundary without claiming lifecycle control.
- [x] 4.2 Run focused tests, OpenSpec validation, diff checks, and the FUNCTION_TREE validator before archive and commit.
