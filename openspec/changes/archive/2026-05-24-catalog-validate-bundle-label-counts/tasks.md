# Tasks: Catalog Validate Bundle Label Counts

## 1. Tests

- [x] 1.1 Add focused failing tests for detailed `bundle_label_counts`.
- [x] 1.2 Add summary-view assertions that preserve `bundle_label_counts` without exposing full bundle data.

## 2. Implementation

- [x] 2.1 Add deterministic selected-bundle label aggregation to `_validate_catalog_registry()`.
- [x] 2.2 Project `bundle_label_counts` through catalog validate summary view.

## 3. Registry And Verification

- [x] 3.1 Update `FUNCTION_TREE.md` E-11 evidence and boundary without claiming execution availability.
- [x] 3.2 Run focused tests, OpenSpec validation, diff checks, GitNexus change detection, and the FUNCTION_TREE validator.
