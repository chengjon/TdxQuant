## 1. Task Expansion

- [x] 1.1 为 `TdxTaskManager` 增加 `watchlist_export`，复用批量总览并落地导出文件。
- [x] 1.2 为 `TdxTaskManager` 增加 `sector_research_export`，复用板块研究并落地导出文件。

## 2. CLI Expansion

- [x] 2.1 为 `task` 二级命令增加 `watchlist-export`。
- [x] 2.2 为 `task` 二级命令增加 `sector-research-export`。

## 3. Profiles And Docs

- [x] 3.1 为导出类 task 增加默认 profile 和导出目录约定。
- [x] 3.2 更新 task 层使用文档与示例。

## 4. Verification

- [x] 4.1 补充导出类 task manager 与 task CLI 测试。
- [x] 4.2 运行定向测试验证新增导出 task 不影响现有 task/manager 入口。
