## 1. Runtime Discovery

- [x] 1.1 建立 Python 项目骨架和 Windows 专用模块目录，加入 `pywin32` 依赖声明。
- [x] 1.2 实现平安证券客户端路径解析，支持显式配置、Windows 路径和 WSL 路径候选探测。
- [x] 1.3 实现 `health-check` 的路径验证输出，确认 `/mnt/d/ProgramData/PinganSec/TdxW.exe` 与 `D:\ProgramData\PinganSec\TdxW.exe` 的映射语义。

## 2. Window Inspection

- [x] 2.1 实现主窗口查找和递归子控件枚举，输出句柄、类名、标题、父句柄和位置等结构化信息。
- [x] 2.2 提供 `inspect` 命令或脚本，用于导出控件树并人工确认平安证券买入页的真实控件结构。
- [x] 2.3 根据真实控件树定义买入页关键控件匹配规则，至少覆盖代码框、价格框、数量框和买入按钮。

## 3. Order Entry Adapter

- [x] 3.1 定义 `BrokerAdapter`、`OrderRequest`、`OrderResult` 等基础模型，并实现 `PingAnBrokerAdapter.detect()`。
- [x] 3.2 实现 `buy` 的前置校验，包括页面状态、证券代码格式、价格格式和数量约束。
- [x] 3.3 实现后台写入与点击流程，并提供 dry-run 或“仅填充不点击”模式用于首轮验证。

## 4. Verification and Safety

- [x] 4.1 为 `inspect`、`detect`、`health-check`、`buy` 输出统一的结构化结果和错误码。
- [ ] 4.2 在真实客户端上验证前台、最小化和遮挡场景，并记录控件识别差异。
- [x] 4.3 补充 README 或使用文档，明确执行环境限制、路径配置方式和未覆盖的风险项。

## 5. Broker Divergence Validation

- [x] 5.1 记录平安证券真实结论：当前版本支持原生控件探测、填单和状态读取，但未验证出纯非物理最终提交闭环。
- [x] 5.2 增加 `hwnd` 级低层命令，支持直接读取、写入和非物理点击 Win32 控件，用于真实客户端排障。
- [x] 5.3 在通达信真实客户端上固化买入页句柄映射，验证代码、价格、数量和提交按钮的实际作用。
- [x] 5.4 基于通达信买入页映射，验证“低价不成交但流程走完”的非物理提交路径，并记录是否出现确认弹窗。
- [x] 5.5 第一轮通达信验证已确认 `买入下单` 按钮可接收 `BM_CLICK`，但提交前后未出现新窗口/确认框变化；需要补强 `Edit` 读回能力后再继续归因。
- [x] 5.6 已验证 `Stock` 注册消息可以切换主证券上下文，但不能替代交易页证券代码输入。
- [x] 5.7 已验证 `WM_SETTEXT`、`WM_CHAR`、`Tab`、`keybd_event` 等 Win32/UIA 级方案都未能让交易业务层接受证券代码输入。

## 6. HID Pivot

- [x] 6.1 设计 HID 路线的总体架构：WSL/策略侧、Windows 主机侧代理、HID 设备三层边界与职责。
- [x] 6.2 选定最小硬件基线，例如 Arduino Leonardo 或兼容 HID 板卡，并记录刷写/部署前提。
- [x] 6.3 设计 Windows 主机侧代理与 HID 设备的最小通信协议，只覆盖“输入证券代码”和“确认键序列”。
- [x] 6.4 复用现有 Win32 探测链路，只把通达信证券代码输入环节替换为 HID 真实键盘事件。
- [ ] 6.5 在真实通达信上验证 HID 输入后，交易弹窗是否从“请输入证券代码!”推进到真正的下单确认。
- [x] 6.6 更新文档，明确 Win32/UIA 路线的能力边界与 HID 作为后续主路径的决策依据。
