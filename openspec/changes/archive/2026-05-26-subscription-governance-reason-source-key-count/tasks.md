# Tasks

## 1. Contract

- [x] 1.1 Add OpenSpec proposal, design, and subscription summary delta for top-level reason source key count.
- [x] 1.2 Validate the active OpenSpec change before implementation.

## 2. Tests

- [x] 2.1 Add subscription watch status summary assertions for `governance.reason_source_key_count`.
- [x] 2.2 Preserve HTTP/CLI summary projection expectations for the additive field.

## 3. Implementation

- [x] 3.1 Derive `governance.reason_source_key_count` from `governance.reason_source_counts`.

## 4. Registry and Verification

- [x] 4.1 Update `FUNCTION_TREE.md` B-16/E-09 evidence and boundary without claiming lifecycle control.
- [x] 4.2 Run focused tests, OpenSpec validation, diff checks, and the FUNCTION_TREE validator before archive and commit.
