# FUNCTION_TREE

> 本文件是 TdxQuant 当前唯一的功能全景图与状态注册表。
>
> 本文件同时记录“当前可用能力”和“已设计/待实现能力”。凡是尚未可用的节点，必须显式标为 `[已设计/待实现]`，并在边界中写明“不可作为当前可用能力使用”。
>
> 不再单独维护会与本文件抢真相的 `ROADMAP.md`。后续路线、缺口和设计项统一登记在本文件的状态注册表中。

## 0. 注册规则

| 字段 | 规则 |
| --- | --- |
| 状态 | 每个功能节点必须使用 `[已实现]`、`[部分实现]`、`[已设计/待实现]`、`[非目标/边界]` 之一。 |
| 证据 | 已实现节点必须指向源码、配置、测试、运行时样例或已归档契约文档；待实现节点必须指向设计/下一步文档或明确缺口来源。 |
| 边界 | 必须说明这个节点当前能用到哪里、不能承诺什么。待实现节点必须明确写出“不代表当前可用”。 |
| 更新方式 | 新功能、新任务、新报告、新 bridge、新交易能力、新契约缺口，都先更新本文件，再删除或归并会形成第二套真相的规划文档。 |

## 1. 当前基线

| 功能节点 | 状态 | 证据 | 边界 |
| --- | --- | --- | --- |
| 项目基线 | `[已实现]` | 当前工作区；`tdxquant_py=65`、`tests=24`、`provider_fixtures=40` | 本文件描述当前工作区主线，不保证外部分支或历史 PR 一致。 |
| 单一功能注册表 | `[已实现]` | 本文件；`docs/TdxQuant_Project_Function_Map.md` 已作为输入资料归并 | `docs/TdxQuant_Project_Function_Map.md` 可作历史/背景参考，但本文件才是功能状态入口。 |
| 运行环境定位 | `[已实现]` | `README.md`：当前分支以 `WSL <-> Windows TDX bridge` 为主目标 | WSL 侧消费结构化 JSON；不直接承诺在 WSL 内操作 Win32/UIA/HID。 |

## 2. 功能全景图

```text
TdxQuant
├── A. 统一入口与治理 [已实现为主]
│   ├── CLI 命令面 [已实现]
│   ├── Manager API [已实现]
│   ├── runtime profiles / presets [已实现]
│   └── command catalog / bundle / report preset [已实现]
├── B. 查询与运行时主线 [已实现为主]
│   ├── market / meta / financial / transaction [已实现]
│   ├── formula [已实现；更多 capability contract 部分覆盖]
│   ├── block [部分实现]
│   ├── runtime 基础能力 [已实现]
│   ├── subscription session / watch / worker bridge [部分实现]
│   └── provider contract / fixtures / replay [部分实现]
├── C. 任务、报告、目录主线 [已实现为主]
│   ├── TdxTaskManager [已实现]
│   ├── task profiles / presets [已实现]
│   ├── report presets [已实现]
│   └── command catalog / command bundles [已实现]
├── D. 桌面自动化交易主线 [部分实现]
│   ├── desktop UIA / Win32 / HID primitives [已实现]
│   ├── TdxTradeManager / TradeService / PingAn gateway [已实现]
│   ├── PingAn buy / sell / submit_once / confirm [部分实现]
│   └── trade audit / ledger / safety governance [部分实现]
└── E. 待闭合与下一阶段能力 [分状态登记]
    ├── subscription query-style one-shot CLI
    ├── subscription HTTP/SSE 推送语义
    ├── provider HTTP replay service / daemon fake provider
    ├── block 文件导入式 watchlist 与写策略硬化/高阶入口
    ├── formula capability-specific contract 状态注册表
    ├── 更厚的日常 task/report/catalog 组合入口
    ├── 更高阶 trade_audit 聚合与跨 ledger 查询
    └── 桌面交易扩展 broker capability 边界
```

## 3. 状态注册表

### A. 统一入口与治理

| ID | 功能节点 | 状态 | 证据 | 边界 |
| --- | --- | --- | --- | --- |
| A-01 | CLI 命令面 | `[已实现]` | `tdxquant/cli.py` 注册约 `204` 个 parser；`tests/test_api_cli.py`、`tests/test_trader_cli.py` | 命令存在不等于每个命令都可在非 Windows 环境完成真实外部调用；外设/TDX 客户端相关命令依赖本机环境。 |
| A-02 | API Manager 统一入口 | `[已实现]` | `tdxquant/api/manager.py`：`TdxApiManager` 与 market/meta/formula/financial/transaction/runtime/block proxy | 统一入口主要封装本地/bridge 调用；不声明覆盖 TDX 原始接口全集。 |
| A-03 | Task Manager 统一入口 | `[已实现]` | `tdxquant/api/task.py`：`TdxTaskManager`；`runtime/task-profiles.json` 约 `22` 个 profile；`runtime/task-presets.json` 约 `11` 个 preset | 已有日常任务入口，但更厚的组合任务仍按具体节点登记为待实现。 |
| A-04 | Trade Manager 统一入口 | `[已实现]` | `tdxquant/trade/manager.py`：`TdxTradeManager`；`tdxquant/trader/service.py`：`TradeService`；`tests/test_trade_manager.py` | 当前重点是平安证券桌面链路；不等于多券商全量交易平台。 |
| A-05 | runtime profiles / presets | `[已实现]` | `runtime/api-profiles.json`、`runtime/task-profiles.json`、`runtime/trade-profiles.json`、`runtime/report-presets.json`、`runtime/command-catalog.json` | JSON 配置是本地入口编排层；外部服务或真实交易环境仍需对应 runtime 前置条件。 |
| A-06 | command catalog | `[已实现]` | `tdxquant/catalog.py`；`runtime/command-catalog.json` 约 `115` 个 entry；`runtime/TdxQuant_Command_Catalog_Usage.md` | catalog 描述可运行命令和参数编排，不替代能力状态判断；状态以本文件为准。 |
| A-07 | command bundle | `[已实现]` | `runtime/command-bundles.json` 约 `144` 个 bundle；`tdxquant/catalog.py` 支持 bundle 解析 | bundle 是组合执行入口；单个 step 仍受底层功能节点边界约束。 |
| A-08 | OpenSpec 生命周期材料 | `[已实现]` | `openspec/changes/archive/**`、`openspec/specs/**`；`docs/superpowers/plans/**`；`scripts/validate_function_tree_registry.py` 与 `tests/test_function_tree_registry.py` 校验 `FUNCTION_TREE.md` 状态/证据/边界列、重复 ID、待实现边界措辞、OpenSpec evidence id 是否存在 active/archive 材料、显式本地证据路径是否存在、根目录 `ROADMAP.md` 缺席，以及 `--json` 成功/失败机器可读报告；OpenSpec `function-tree-registry-validator` / `function-tree-openspec-evidence-validation` / `function-tree-evidence-path-validation` / `function-tree-validator-json-report` / `function-tree-lifecycle-status-registry` | `[已实现]` 仅指 OpenSpec lifecycle 材料与 FUNCTION_TREE registry validator/报告能力已经有源码、测试、spec 和边界；可作为设计、归档和单一功能注册表结构/报告校验证据，不直接证明当前代码可运行；validator 只校验 `FUNCTION_TREE.md` 的状态注册表形状、OpenSpec evidence 引用存在性、保守识别的显式本地证据路径存在性、“无 ROADMAP.md 抢真相”约束和 JSON 报告结构；不会执行证据路径、解释 glob/命令/符号/自由文本证据，也不证明对应功能可用，`--json` 只是同一校验结果的机器可读投影，其他功能节点实现状态仍以各自源码/测试/运行时配置为准。 |

### B. 查询与运行时主线

| ID | 功能节点 | 状态 | 证据 | 边界 |
| --- | --- | --- | --- | --- |
| B-01 | market 查询 | `[已实现]` | `tdxquant/api/market.py`；`tdxquant/api/bridge.py`：`run_tdx_market_snapshot`、`run_tdx_data_kline`、`run_tdx_full_tick`；provider fixture `market-*.json` | 依赖 TDX/bridge provider；fixture 只能证明契约和 replay，不代表实时行情环境永远可用。 |
| B-02 | meta 查询 | `[已实现]` | `tdxquant/api/meta.py`；provider fixture `meta-stock-list-success.json`、`meta-sector-stocks-success.json` | 覆盖股票列表、板块股票等常用元数据；不声明覆盖所有 TDX 字典类接口。 |
| B-03 | financial 查询 | `[已实现]` | `tdxquant/api/financial.py`；`tdxquant/api/bridge.py`：`run_tdx_financial_data`；provider fixture `financial-financial-data-*.json` | 已有财务数据桥接和 fixture；更高阶基本面分析不是当前节点承诺。 |
| B-04 | transaction 查询 | `[已实现]` | `tdxquant/api/transaction.py`；CLI parser 包含 `stock-transaction-data`、`sector-transaction-data`、`market-transaction-data` | 当前是查询入口层；性能、长时间批量拉取和缓存策略按 runtime 节点边界处理。 |
| B-05 | formula 基础接口 | `[已实现]` | `tdxquant/api/formula.py`；CLI parser 包含 `formula-format-data`、`formula-set-data`、`formula-zb`、`formula-xg` 等 | 已有常用公式入口；“公式类 capability 全覆盖”另列为部分覆盖/待实现，不在此处偷换为已完成。 |
| B-06 | formula.screen provider contract | `[已实现]` | `tdxquant/formula_screen.py`；provider fixture `formula-screen-success.json`、`formula-screen-failure.json`；相关契约文档 `docs/TdxQuant_Provider_Formula_Screen_Contract.md` | 已形成 provider contract；真实公式执行仍依赖 Windows 原生 TDX 环境。 |
| B-07 | formula capability-specific contract 状态注册表 | `[已实现]` | `tdxquant/formula_capabilities.py` 登记 `formula.screen` 与 legacy formula bridge 能力的 status/evidence/boundary；`TdxApiManager.formula.capabilities()` 与 `api formula-capabilities` 提供只读发现入口；`tests/test_api_manager.py`、`tests/test_api_cli.py` 覆盖 provider-stable 与 bridge-only 分流；OpenSpec `formula-capability-contract-registry` | 这是状态注册表和发现入口，不新增公式执行能力；当前只有 `formula.screen` 标记为 provider-contract-stable，`formula.xg` / `formula.zb` / `formula.get_data` 等仍是 bridge-only，不能作为稳定 provider/replay contract 使用。 |
| B-08 | block 用户板块读取/生命周期变更 | `[已实现]` | `tdxquant/api/block.py`、`tdxquant/block_mutation.py`；provider fixture `block-send-user-block-*.json`；`tests/test_block_sync.py` | 已支持用户板块读写相关基础能力；真实写入必须经过 mutation governance 和 dry-run/plan 约束。 |
| B-09 | block.read_watchlist_snapshot | `[已实现]` | `tdxquant/block_snapshot.py`；provider fixture `block-read-watchlist-*.json`；`tests/test_block_snapshot.py` | 当前是 watchlist 快照读取契约；不等价于任意格式导入或复杂合并写入。 |
| B-10 | block.sync_watchlist | `[已实现]` | `tdxquant/block_sync.py`；provider fixture `block-sync-*.json`；`tests/test_block_sync.py` | 已支持受控同步；覆盖写/增量写高阶入口按 E-04 登记，并继续受 provider mutation 安全边界约束。 |
| B-11 | block mutation audit / mutation_key governance | `[已实现]` | `tdxquant/block_mutation.py`；`docs/TdxQuant_Provider_Block_Mutation_Safety.md` | governance 是安全边界，不是绕过真实 TDX 客户端限制的保证。 |
| B-12 | runtime 基础能力 | `[已实现]` | `tdxquant/api/runtime.py`；CLI parser 包含 `refresh-cache`、`trading-dates`、`refresh-kline`、`download-file`、`send-warn` | 基础 runtime 命令已存在；`send-warn` 等能力不是当前主接入路径，使用前需核对本机配置。 |
| B-13 | persistent subscription session | `[已实现]` | `tdxquant/api/bridge.py`：`TdxRuntimeSubscriptionSession`、`run_tdx_open_subscription_session`；`tests/test_subscription_event_contract.py` | 提供 subscribe/unsubscribe/list 等 session 能力；长期运行治理仍按 B-16/E-01 分状态。 |
| B-14 | task subscription-watch 前台运行 | `[已实现]` | `tdxquant/subscription_watch_run.py`；`tests/test_subscription_watch_run.py`；`docs/TdxQuant_Task_Subscription_Watch_Contract.md` | 已有 bounded run、artifact contract；长跑稳定性和 reconnect/backoff 治理仍属部分覆盖。 |
| B-15 | worker bridge background control plane | `[已实现]` | `tdxquant/subscription_watch_background.py`、`tdxquant/subscription_watch_background_runner.py`、`tdxquant/bridge_http.py`、`tdxquant/bridge_registry.py`；`runtime/bridge/*.example.json`；`tests/test_bridge_http.py`、`tests/test_bridge_registry.py` | 已有 worker-local single-active 与 HTTP control plane；生产部署仍需 token、allowlist、进程管理和 Windows 环境配置。 |
| B-16 | reconnect/backoff 与长期运行治理 | `[部分实现]` | `build_subscription_watch_status_summary()` 汇总 `control_rollup`、`reconnecting/degraded`、heartbeat、watermark、reconnect 字段，支持显式 `heartbeat_stale_after_seconds`、`watermark_stale_after_seconds` 与 `reconnect_stale_after_seconds` 的 fresh/stale/age/evaluated_at 诊断，并新增 advisory `governance.decision/requires_manual_review/reasons/reason_count/actions/action_count/action_summary(primary_reason_source/primary_severity/reason_source_counts/reason_code_counts)/evaluation_summary/component_status_counts/evaluated_status_counts/reconnect_rollup/staleness_evaluated/boundary/reason_source_counts/reason_summary(reason_code_counts)`；`SubscriptionWatchBackgroundController.status()`、bridge HTTP `watch/status?heartbeat_stale_after_seconds=...&watermark_stale_after_seconds=...&reconnect_stale_after_seconds=...`、bridge HTTP `watch/status?view=summary`、`bridge watch-status --heartbeat-stale-after-seconds --watermark-stale-after-seconds --reconnect-stale-after-seconds --view summary` 已接出，summary view 投影 `status_summary.control_rollup`、`runtime.control_state/active/watch_state/run_id/pid`、`governance.staleness_evaluated`、`governance.boundary`、`governance.reason_count`、`governance.action_count`、`governance.reason_source_counts`、`governance.reason_summary.reason_code_counts`、有界 `governance.reason_samples` / `reason_sample_limit` / `reason_sample_truncated`、有界 `governance.action_samples` / `action_sample_limit` / `action_sample_truncated`、`governance.action_summary`（含 `primary_reason_source` / `primary_severity` / `severity_counts` / `action_name_counts` / `reason_source_counts` / `reason_code_counts`）、`governance.reconnect_rollup` 与 `governance.evaluation_summary`（含 `evaluated_components` / `evaluated_count` / `stale_components` / `stale_count` / `fresh_components` / `fresh_count` / `not_evaluated_components` / `not_evaluated_count` / `component_status_counts` / `evaluated_status_counts`）；`tests/test_subscription_watch_background.py`、`tests/test_bridge_http.py`、`tests/test_bridge_registry.py`、`tests/test_api_cli.py`；OpenSpec `subscription-long-run-status-summary` / `subscription-watch-heartbeat-staleness-summary` / `subscription-watch-watermark-staleness-summary` / `subscription-watch-reconnect-staleness-summary` / `subscription-watch-governance-summary` / `subscription-governance-action-summary` / `subscription-governance-review-flag` / `subscription-governance-action-rollup` / `subscription-governance-action-primary-reason-source` / `subscription-governance-action-primary-severity` / `subscription-watch-status-summary-view` / `subscription-watch-status-http-summary-view` / `subscription-summary-staleness-evaluated-view` / `subscription-summary-runtime-identity-view` / `subscription-governance-evaluation-summary` / `subscription-summary-governance-boundary-view` / `subscription-summary-governance-reason-count-view` / `subscription-summary-governance-reason-samples-view` / `subscription-summary-governance-action-samples-view` / `subscription-governance-action-severity-summary` / `subscription-governance-reason-source-counts` / `subscription-summary-fresh-evaluation-counts` / `subscription-evaluation-component-list-counts` / `subscription-governance-detailed-reason-count` / `subscription-governance-action-name-counts` / `subscription-governance-action-reason-source-counts` / `subscription-governance-action-reason-code-counts` / `subscription-governance-action-count` / `subscription-evaluation-component-status-counts` / `subscription-evaluation-evaluated-status-counts` / `subscription-governance-reason-code-counts` / `subscription-governance-reconnect-rollup` / `subscription-control-rollup-summary` | 已有状态摘要、显式 heartbeat/watermark/reconnect stale 诊断、advisory governance posture/action hints/action rollup/primary action reason source/primary severity/manual-review flag、只读 CLI summary view 和只读 HTTP summary view 部分契约；summary view 的 `runtime` 只从既有 `control`/`watch_status` 派生选定标量身份字段，不暴露 raw payload，也不代表生命周期控制；`status_summary.control_rollup` 只从已传入的 reconciled `control` payload 投影 control state、active、run_id/pid presence 与 terminal reason flags，不读取 lock、不证明 PID liveness、process ownership、supervisor ownership、readiness、statefile 新旧程度或 lifecycle control；默认不做 wall-clock stale 推断，未传阈值时保持 `not_evaluated`/`observe`/`requires_manual_review=false`/`staleness_evaluated=false`/`action_count=0`/`action_summary.count=0`/`action_summary.primary_reason_source=null`/`action_summary.primary_severity="none"`/`evaluated_count=0`/`stale_count=0`/`fresh_count=0`/`severity_counts={}`/`action_name_counts={}`/`action_summary.reason_source_counts={}`/`action_summary.reason_code_counts={}`/`evaluated_status_counts={}`，并由 `evaluation_summary.not_evaluated_components` / `not_evaluated_count` 标注未评估分量；`evaluation_summary.evaluated_components` / `evaluated_count` / `stale_components` / `stale_count` / `fresh_components` / `fresh_count` / `not_evaluated_components` / `not_evaluated_count` / `component_status_counts` / `evaluated_status_counts` 只是从显式评估分量中派生的组件列表、计数和状态分布摘要，`evaluated_status_counts` 排除 `not_evaluated`，不是健康保证、自动恢复条件或生产治理策略；`governance.reconnect_rollup` 只是从既有 `reconnect` 诊断投影 staleness、reconnect/failure 计数、last-error/next-reconnect presence、age source 与阈值，不读取 statefile/lock、不证明进程 ownership、supervisor 存活、backoff 调度或 restart readiness；`governance.boundary` 只是 compact summary 中保留的 advisory-only 边界标记，详细 payload 和 summary view 的 `governance.reason_count` 都只是从详细 `governance.reasons` 派生的数量，`governance.action_count` 都只是从详细 `governance.actions` 派生的数量，`governance.reason_source_counts` 只是按 reason 前缀聚合的来源计数，`governance.reason_summary.reason_code_counts` 只是按完整 advisory reason 字符串聚合的计数，不是完整 reasons 清单或治理策略，`governance.reason_samples` 只是最多 3 个代表样本，不是完整 reasons 清单；`governance.action_samples` 只是最多 3 个去掉 description 的 compact action 样本，不是完整 actions 清单或执行指令；`governance.action_summary.primary_severity` 只是第一条 advisory action 的 severity，与兼容字段 `severity` 同口径，不是执行等级、自动升级策略或自动化治理；`governance.action_summary.severity_counts` 只是 advisory actions 的 severity 聚合，不是执行等级或自动化策略；`governance.action_summary.action_name_counts` 只是 advisory actions 的 action 名称聚合，不是完整 actions 清单、执行指令或治理策略；`governance.action_summary.primary_reason_source` 只是第一条 advisory action reason 的来源前缀；`governance.action_summary.reason_source_counts` 只是 advisory action reasons 的来源前缀聚合，`governance.action_summary.reason_code_counts` 只是 advisory action reason 完整字符串聚合，不是完整 actions 清单、执行指令或治理策略；`governance.actions`、`reason_count`、`action_count`、`reason_source_counts`、`reason_summary.reason_code_counts`、`reason_samples`、`action_samples`、`staleness_evaluated`、`action_summary`、`reconnect_rollup`、`evaluation_summary`、`requires_manual_review` 与 HTTP/CLI summary view 仅把人工复核理由、建议动作、运行身份和是否已评估 stale 阈值结构化为只读建议，不自动触发 reconnect/backoff/restart，也未完全收口长期治理或生产进程管理。 |
| B-17 | provider result contract | `[已实现]` | `tdxquant/result_contract.py`、`tdxquant/query_contract.py`；provider fixture `provider-result-*.json`；`tests/test_provider_result_contract.py` | 约束 provider 返回结构；不保证外部 provider 永不返回业务异常。 |
| B-18 | provider capability discovery / health / doctor | `[已实现]` | `tdxquant/provider_discovery.py`；`tdxquant/api/bridge.py`：`run_tdx_provider_capabilities`、`run_tdx_provider_health`、`run_tdx_provider_doctor`；fixture `runtime-capabilities-success.json`、`runtime-health-degraded.json`、`runtime-doctor-degraded.json` | 发现和诊断是可观测能力；不自动修复本机 TDX、COM、窗口或行情源问题。 |
| B-19 | provider fixtures / in-process replay | `[已实现]` | `tdxquant/fixtures/provider/*` 共 `57` 个 fixture；`tdxquant/replay_fixtures.py`、`tdxquant/replay_provider.py`；`tests/test_replay_fixtures.py`、`tests/test_replay_provider.py` | fixture/replay 用于契约测试和离线验证；不能替代真实行情、公式或交易联调。 |

### C. 任务、报告、目录主线

| ID | 功能节点 | 状态 | 证据 | 边界 |
| --- | --- | --- | --- | --- |
| C-01 | TdxTaskManager | `[已实现]` | `tdxquant/api/task.py`：`TdxTaskManager`；`runtime/TdxQuant_Task_Layer_Usage.md` | 提供任务编排层；单个任务的真实能力仍受对应 B/D 节点边界约束。 |
| C-02 | task profiles / presets | `[已实现]` | `runtime/task-profiles.json` 约 `21` 个 profile；`runtime/task-presets.json` 约 `13` 个 preset | profiles/presets 是默认参数和组合入口，不保证所有环境一键完成。 |
| C-03 | watchlist task/report 入口 | `[已实现]` | CLI parser 包含 `watchlist-overview`、`watchlist-export`、`block-read-watchlist`、`block-read-full`、`block-read-watchlist-export`；`docs/TdxQuant_Project_Function_Map.md` 记录 `block-read-full` 完成态 | 以当前 block 读取/同步能力为基础；文件导入式 watchlist 以 E-03 的 import-only 边界为准。 |
| C-04 | subscription-watch task 入口 | `[已实现]` | CLI parser 包含 `subscription-watch`；`tdxquant/subscription_watch_run.py`；`tests/test_subscription_watch_run.py` | 前台 run 和 artifact 契约已可用；更高层 long-running product wrapper 仍按 E-06 登记。 |
| C-05 | report presets | `[已实现]` | `runtime/report-presets.json` 约 `101` 个 preset；`tdxquant/reporting.py` | 报表是本地 artifact 汇总/查询入口；数据完整性取决于对应 runtime/trade/task artifact 是否已生成。 |
| C-06 | trade report / audit report 入口 | `[已实现]` | CLI parser 包含 `daily-trade-report`、`trade-report-lookup`、`trade-audit-lookup`、`trade-audit-daily-report`、`trade-audit-period-report`、`trade-period-report` | 已有日常诊断和回看入口；更高阶跨 ledger 组合查询仍按 E-07 待实现。 |
| C-07 | command catalog list/plan/run | `[已实现]` | CLI parser 包含 `catalog`、`list`、`plan`、`run`；`tdxquant/catalog.py` | catalog/bundle 的 plan/run 不绕过底层能力边界；失败原因需回到具体节点诊断。 |
| C-08 | read-zxg-review-and-export bundle | `[已实现]` | `runtime/command-bundles.json` 包含 `read-zxg-review-and-export`；`docs/TdxQuant_Project_Function_Map.md` 记录该 bundle 完成态 | 以现有 watchlist/block 能力为基础；不是任意外部文件导入能力。 |

### D. 桌面自动化交易主线

| ID | 功能节点 | 状态 | 证据 | 边界 |
| --- | --- | --- | --- | --- |
| D-01 | desktop UIA primitives | `[已实现]` | `tdxquant/desktop/uia.py`、`tdxquant/uia_inspector.py`；CLI parser 包含 `uia-*` 命令 | 依赖 Windows 桌面、目标窗口和控件树稳定性；WSL 侧只能通过 bridge/JSON 消费结果。 |
| D-02 | desktop Win32 primitives | `[已实现]` | `tdxquant/desktop/win32.py`、`tdxquant/win32_api.py`；CLI parser 包含 `win32-*` 命令 | 仅适用于 Windows 客户端自动化；窗口标题、权限、焦点和消息投递策略会影响结果。 |
| D-03 | HID bridge primitives | `[已实现]` | `tdxquant/desktop/hid.py`、`tdxquant/hid_bridge.py`；CLI parser 包含 `hid-ping`、`hid-send`、`tdx-trade-hid-*`；`tests/test_hid_bridge.py` | 依赖 HID/串口设备和本机端口；测试可覆盖契约，不代表硬件永远在线。 |
| D-04 | broker / gateway abstraction | `[已实现]` | `tdxquant/trader/gateway.py`、`tdxquant/trader/registry.py`、`tdxquant/trader/models.py`、`tdxquant/brokers/base.py` | 抽象已存在；实际可用 broker 以已接入 adapter 为准。 |
| D-05 | PingAn desktop gateway | `[已实现]` | `tdxquant/trader/adapters/pingan_desktop.py`：`PingAnDesktopTraderGateway`；`tests/test_pingan_trader_gateway.py`、`tests/test_trader_gateway.py` | 当前主要服务平安证券桌面客户端；其他券商不能按此节点默认视为可用。 |
| D-06 | PingAn health / preflight / readiness | `[已实现]` | `TdxTradeManager.pingan.health/preflight/submit_ready/dialog_readiness`；CLI parser 包含 `trade health`、`trade preflight`、`submit-ready`、`dialog-readiness` | 是交易前诊断与准备检查；通过检查不等于实际下单一定成功。 |
| D-07 | PingAn buy / sell / confirm_current | `[部分实现]` | `TdxTradeManager.pingan.buy/sell/confirm_current`；稳定 CLI/task 入口包含 `trade buy`、`trade sell`、`task trade-buy`、`task trade-sell`、`trade confirm-current`；`task-buy-default` / `task-sell-default` / `confirm-current-default` preset、`task-buy` / `task-sell` / `task-confirm-current` catalog entry、`buy-pingan-complete-review`、`buy-pingan-*-review`、`sell-pingan-complete-review`、`sell-pingan-*-review`、`confirm-current-pingan-complete-review` 与 `confirm-current-pingan-*-review` follow-up bundle 已登记；`catalog validate --kind bundle --label pingan --view summary` 可非执行输出 `pingan_bundle_count`、有界 `pingan_bundle_samples`、`pingan_bundle_sample_limit` 与 `pingan_bundle_sample_truncated`；`catalog plan --entry task-sell --view summary` 与 trade follow-up bundle summary 可投影 `trade_plan_boundary`，标注 `trade_command`、`execution_mode=non_executing_catalog_plan`、`dispatch_executed=false`、required/provided/missing input fields、`required_input_count` / `provided_input_count` / `missing_input_count` 及 `input_coverage_status`；`tdxquant/trade/manager.py`、`tdxquant/api/task.py`；`runtime/trade-audits/*`；`tests/test_trade_manager.py`、`tests/test_api_cli.py`、`tests/test_api_manager.py`；OpenSpec `trade-sell-stable-entry` / `trade-sell-catalog-followup-registry` / `trade-sell-pingan-complete-review` / `trade-buy-pingan-complete-review` / `trade-buy-pingan-exception-review` / `confirm-current-pingan-followup-registry` / `confirm-current-pingan-complete-review` / `command-catalog-validate-pingan-samples` / `catalog-trade-plan-boundary-summary-view` / `catalog-trade-plan-input-count-summary` / `catalog-trade-plan-input-coverage-status` | 已有买入、卖出、确认链路、稳定 buy / sell / confirm_current 入口、catalog 发现入口和审计；真实交易仍必须显式提供订单参数并受 max_price、submission_key、人工/环境安全约束；`buy-pingan-complete-review`、`buy-pingan-*-review`、`sell-pingan-complete-review`、`sell-pingan-*-review`、`confirm-current-pingan-complete-review` 与 `confirm-current-pingan-*-review` 只是复用既有 task/report entry 的固定组合，不代表新增桌面执行原语；`catalog validate` 的 PingAn 样本只证明固定 bundle 定义可解析和可采样，不代表真实执行可用；`trade_plan_boundary` 只说明 catalog plan/preview 已解析 trade command 与输入字段覆盖，并显式标注非执行和未 dispatch，input counts 与 `input_coverage_status` 只是从 required/provided/missing 字段列表派生的 catalog 输入覆盖摘要，不代表真实交易可用性、broker readiness 或安全审批；不代表所有交易品种、券商、异常弹窗或结果弹窗分支都已覆盖。 |
| D-08 | PingAn submit_once | `[部分实现]` | `TdxTradeManager.pingan.buy_submit_once/sell_submit_once`；`trade submit-once --side buy/sell` 与 `task trade-submit-once --side buy/sell` 显式暴露 side 边界，默认 buy；`PingAnDesktopTraderGateway(execution_mode="submit_once", side=sell)` 与 `TdxTaskManager.trade_submit_once(side="sell")` 走 `sell_submit_once` 管理器/审计身份；`buy-submit-once-default` / `sell-submit-once-default` preset、`task-buy-submit-once` / `task-sell-submit-once` catalog entry、`buy-submit-once-pingan-complete-review` / `buy-submit-once-pingan-*-review`、`sell-submit-once-pingan-complete-review` 与 `sell-submit-once-pingan-*-review` bundle 已登记；`audit-daily/period-pingan-sell-submit-once-*` report preset、catalog entry；`catalog validate --kind bundle --label submit-once --view summary` 可非执行输出 `submit_once_bundle_count`、有界 `submit_once_bundle_samples`、`submit_once_bundle_sample_limit` 与 `submit_once_bundle_sample_truncated`；`catalog plan --entry task-sell-submit-once --view summary` 与 submit-once bundle summary 可投影 `trade_plan_boundary`，标注 `trade_command=trade-submit-once`、`side`、`execution_mode=non_executing_catalog_plan`、`dispatch_executed=false`、required/provided/missing input fields、`required_input_count` / `provided_input_count` / `missing_input_count` 及 `input_coverage_status`；`tests/test_trade_manager.py`、`tests/test_pingan_trader_gateway.py`、`tests/test_api_cli.py`、`tests/test_api_manager.py`；OpenSpec `trade-submit-once-side-boundary` / `trade-sell-submit-once-entry` / `sell-submit-once-audit-registry` / `trade-submit-once-sell-catalog-entry` / `trade-sell-submit-once-pingan-complete-review` / `trade-submit-once-buy-catalog-entry` / `trade-submit-once-buy-complete-review` / `command-catalog-validate-submit-once-samples` / `catalog-trade-plan-boundary-summary-view` / `catalog-trade-plan-input-count-summary` / `catalog-trade-plan-input-coverage-status` | 支持当前适配过的 buy/sell submit-once 路径、独立方法身份和 PingAn sell_submit_once audit 发现入口；`sell_submit_once` 仍复用既有 `run_pingan_sell_fast` 桌面流，不代表存在独立 `run_pingan_sell_submit_once` 原语；`task-buy-submit-once` 与 `task-sell-submit-once` 只是显式 side 入口，buy/sell complete-review bundle 只是复用既有 task/report entry，不代表新增另一套桌面执行原语；`catalog validate` 的 submit-once 样本只证明固定 bundle 定义可解析和可采样，不代表真实执行可用；`trade_plan_boundary` 的 side/input coverage 只说明 catalog plan 已解析 submit-once 方向和输入字段，且仍是非执行、未 dispatch 的摘要边界，input counts 与 `input_coverage_status` 只是从 required/provided/missing 字段列表派生的 catalog 输入覆盖摘要，不代表真实交易可用性、broker readiness 或安全审批；不代表所有交易品种、券商、异常弹窗、结果弹窗或非 PingAn sell_submit_once 报表族都已覆盖。 |
| D-09 | trade safety / idempotency governance | `[已实现]` | `tdxquant/trader/store.py`；runtime artifact `runtime/pingan-submission-ledger.jsonl`、`runtime/pingan-order-events.jsonl`；`docs/TdxQuant_Project_Function_Map.md` 记录 `submission_key` ledger、`max_price` 风险门、`trade_safety` 摘要 | governance 用于降低重复提交和危险价格风险；不替代真实交易前的人为风控。 |
| D-10 | immutable trade audit artifacts | `[已实现]` | `runtime/trade-audits/` 当前存在约 `300` 个审计 JSON；`runtime/report-presets.json` 包含大量 audit preset | artifact 可用于回看和诊断；目录数量是当前工作区样例，不保证生产保留策略。 |
| D-11 | trade_audit daily/period diagnostics | `[已实现]` | CLI parser 包含 `trade-audit-daily-report`、`trade-audit-period-report`；`runtime/report-presets.json` 包含 pingan/rejected/failed/exceptions 等 preset；`TdxTaskManager.trade_audit_daily_report` / `trade_audit_period_report` 输出 by_code、by_status、by_day 与 `value_diagnostics` requested-order-value 诊断；`tests/test_api_manager.py` 覆盖 daily/period 过滤、导出与 requested value 聚合；OpenSpec `trade-audit-report-value-diagnostics` | 当前是本地 audit JSON 的只读诊断：金额仅按 audit result data 中已有 `price * quantity` 计算 requested order value 覆盖率；不查询券商、不改 audit/ledger、不推断成交质量、fees、账户余额或 PnL；跨 ledger/count aggregation 仍以 E-12 的只读边界为准。 |

### E. 已设计/待实现能力

| ID | 功能节点 | 状态 | 证据 | 边界 |
| --- | --- | --- | --- | --- |
| E-01 | subscription query-style one-shot CLI | `[已实现]` | `tdxquant/api/bridge.py` 提供 `run_tdx_subscription_subscribe/unsubscribe/list` one-shot wrappers；`RuntimeApi.subscription_subscribe/unsubscribe/list` 与 `TdxApiManager.runtime.subscription_subscribe/unsubscribe/list` 接出 live/replay 路径；`tdxquant/cli.py` 提供 `api subscription-subscribe`、`api subscription-unsubscribe`、`api subscription-list` 并允许 `--provider-mode replay`；fixture `subscription-subscribe-success.json`、`subscription-unsubscribe-success.json`、`subscription-list-success.json`；`tests/test_tdx_api_bridge.py`、`tests/test_api_cli.py`、`tests/test_api_manager.py`、`tests/test_replay_fixtures.py`、`tests/test_replay_provider.py`；OpenSpec `subscription-one-shot-replay-fixtures` | 当前闭合范围仅是 query-style one-shot 方法调用与 fixture-backed replay：live 路径会打开 runtime subscription session、调用一次 `subscribe_hq`/`unsubscribe_hq`/`get_subscribe_hq_stock_list` 并关闭，replay 路径返回确定性 fixture；不启动 foreground `subscription-watch`、background worker、SSE/event-stream transport，也不覆盖 reconnect/backoff 长跑治理。 |
| E-02 | subscription HTTP/SSE 推送语义 | `[已实现]` | `tdxquant/bridge_http.py` 提供 `GET /bridge/v1/watch/events` 与 `GET /bridge/v1/watch/events/stream` read-only 投影；`tdxquant/bridge_registry.py` 提供 master-side events/SSE helpers；`bridge watch-events` 输出 JSON envelope，`bridge watch-events-stream` 输出原始 SSE 文本；`tdxquant/fixtures/provider/subscription-watch-event-stream-frames.jsonl`；`tests/test_bridge_http.py` / `tests/test_bridge_registry.py` / `tests/test_replay_fixtures.py` / `tests/test_api_cli.py`；OpenSpec `bridge-watch-events-cli-entry` | 已实现的是 read-only bridge event/events-stream v1 语义和 CLI 发现入口，投影现有 run artifacts 与 controller state；不重写 `subscription-watch` artifact contract，不改变 worker registry/auth 语义，不启动/停止 worker，也不代表多 worker 调度或更高层协调协议。 |
| E-03 | block 文件导入式 watchlist 适配 | `[已实现]` | `tdxquant/block_watchlist_import.py` 支持 JSON schema、CSV header、TXT directive parser/validator、dry-run plan 与 `sync_watchlist_import_file(...)` 接线；`TdxTaskManager.block_watchlist_import`、`task block-watchlist-import`、`runtime/task-presets.json` 的 `plan-zxg-watchlist-import` 与 catalog entry；示例 `runtime/watchlist-imports/zxg-watchlist-import.example.json/csv/txt`；`tests/test_block_watchlist_import.py` / `tests/test_api_cli.py` 覆盖解析、验证、task dispatch 与 catalog plan；OpenSpec `block-watchlist-import-task-entry` / `block-watchlist-import-text-formats` | 当前是 import-only JSON/CSV/TXT adapter + task/catalog 入口；不含双向同步、源文件回写或任意 spreadsheet 映射，也不绕过现有 `block.sync_watchlist` 安全边界；catalog preset 默认 dry-run 示例，真实写入仍需显式 `--no-dry-run` 和 provider 前置条件。 |
| E-04 | block 覆盖写/增量写高阶任务入口 | `[已实现]` | `tdxquant/block_sync.py` 支持显式 `write_policy`、`mode`/`dry_run` 兼容解析、mutation_key replay/conflict 元数据和 audit policy 字段；`TdxTaskManager.block_sync`、`api/task block-sync --write-policy`、`runtime/task-presets.json` 的 `plan-zxg-block-sync-merge` 与 catalog entry；`tests/test_block_sync.py`、`tests/test_api_cli.py`、`tests/test_tdx_api_bridge.py`；OpenSpec `block-sync-write-policy-hardening` / `block-sync-write-policy-task-entry` | 当前覆盖 API/CLI/task/catalog 的显式写策略入口，catalog preset 默认 `merge_dry_run` 计划；真实写入仍需显式非 dry-run 策略/参数和 provider 前置条件，不新增 provider schema，也不绕过既有 mutation/sync 安全边界。 |
| E-05 | provider HTTP replay service | `[已实现]` | `tdxquant/provider_transport_replay.py` 提供 `GET /provider/v1/replay/health`、`fixtures`、`result`、`watch/status`、`watch/events`、`watch/events/stream`；`provider-replay serve/config-check` CLI；`runtime/provider-transport-replay.example.json`；`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py`；OpenSpec `provider-transport-replay-cli-entry` | 当前是 fixture-backed、read-only、标准库 HTTP replay service；`provider-replay serve` 是前台阻塞入口，不代表后台 daemon 生命周期，不新增业务 capability，不改变 CLI subprocess replay 语义，也不等同于 live Windows provider/bridge。 |
| E-06 | daemon fake provider | `[已实现]` | `tdxquant/provider_transport_replay.py` 提供 `ProviderTransportReplayHTTPServer`、`provider-replay serve/status/config-check/lifecycle-plan/lifecycle-readiness/lifecycle-state-check/daemon start/status/stop/supervise` 所需的 replay fake provider、probe summary、statefile ownership lock、managed daemon control、foreground supervisor、restart/backoff、process ownership diagnostics、managed lifecycle status 与 readiness gate；`tdxquant/cli.py` 暴露对应 CLI；`runtime/provider-transport-replay.example.json` 与 `tdxquant/fixtures/provider/*` 提供离线 replay/fixture 证据；`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 覆盖 fake provider、probe、status summary、statefile、daemon control、supervisor、restart/backoff、ownership diagnostics、managed lifecycle status/readiness；OpenSpec `provider-replay-daemon-supervisor-loop`、`provider-replay-daemon-restart-backoff-policy`、`provider-replay-process-ownership-diagnostics`、`provider-replay-managed-lifecycle-status-summary`、`provider-replay-managed-lifecycle-readiness-gate`、`provider-replay-daemon-lifecycle-implemented-registry`。 | 已实现范围是离线 replay fake provider 与其显式 operator opt-in managed lifecycle：可启动/停止/查询/supervise replay HTTP 进程，可写入/校验本地 lifecycle statefile，可执行 bounded restart/backoff 策略，可做只读 probe/status/readiness/ownership diagnostics；不代表真实 TongDaXin provider 生命周期管理、真实行情会话、broker/workflow/read-write readiness、生产交易可用性或端口 ownership 推断；`status`/`lifecycle-plan`/`lifecycle-readiness` 仍是只读判定，除显式 `provider-replay daemon ...` 子命令外不执行 lifecycle control；其他交易、订阅长期治理和写能力节点仍按各自状态登记。 |
| E-07 | wider capability replay coverage | `[已实现]` | 已有 `57` 个 provider fixture；当前 `_QUERY_CONTRACT_REGISTRY` 的所有 query capability 均 `supports_replay=True` 且有 default fixture mapping，包括 `market-full-tick-success`、`market-market-snapshot-success`、`market-stock-info-success`、`market-more-info-success`、`market-cb-info-success`、`meta-gb-info-success`、`meta-ipo-info-success`、`meta-gp-one-success`、`meta-divid-factors-success`、`meta-sector-list-success`、`transaction-stock-transaction-data-by-date-success`、`transaction-market-transaction-data-by-date-success`、`transaction-sector-transaction-data-by-date-success`、`transaction-sector-transaction-data-success`；对应 `execute_sync_replay(...)`、`TdxApiManager.market/meta/transaction.*(provider_mode="replay")`、`api full-tick/market-snapshot/stock-info/more-info/cb-info/gb-info/ipo-info/gp-one/divid-factors/sector-list/stock-transaction-data-by-date/market-transaction-data-by-date/sector-transaction-data-by-date/sector-transaction-data --provider-mode replay` 与已存在的 `tdx-data-market-snapshot/stock-info/more-info/cb-info/gb-info/ipo-info/gp-one/divid-factors/sector-list/stock-transaction-by-date/market-transaction-by-date/sector-transaction-by-date/sector-transaction --provider-mode replay` 已有 `tests/test_replay_fixtures.py` / `tests/test_replay_provider.py` / `tests/test_api_cli.py` 覆盖；OpenSpec 覆盖 `provider-replay-stock-info-coverage` 至 `provider-replay-full-tick-coverage` 等 replay slice | 已实现范围是“当前已登记 query contract 的 fixture-backed replay 覆盖”；不新增不存在的 flat `tdx-data-full-tick` 命令，不代表未来新增 provider capability 自动具备 replay，未来新增 query contract 仍必须补 fixture、default mapping、query metadata、CLI/manager 测试后才能登记为可用。 |
| E-08 | 更完整 formula capability-specific contract 状态发现 | `[已实现]` | `tdxquant/formula_capabilities.py` 的 registry 汇总 `total/provider_contract_stable/bridge_only/replay_supported`，并由 `TdxApiManager.formula.capabilities()`、`api formula-capabilities` 暴露；`tests/test_api_manager.py` 验证 discovery 不调用公式执行；`tests/test_api_cli.py` 验证 CLI parser/dispatch；OpenSpec `formula-capability-contract-registry` | 已实现的是逐能力状态发现，不是所有 formula 能力的 provider contract；新增 formula 能力仍必须逐项补 contract、fixture、测试后才能从 bridge-only 升级。 |
| E-09 | 更厚 subscription 长跑包装 | `[部分实现]` | `SubscriptionWatchBackgroundController.status()` 返回 additive `status_summary`，汇总 control/watch_status 的 state、heartbeat、watermark、reconnect/degraded 字段，可在显式阈值下计算 heartbeat、watermark 与 reconnect/degraded duration 的 `fresh/stale`、`age_seconds`、`stale_after_seconds`、`evaluated_at`，并通过 `governance.decision/requires_manual_review/reasons/actions/action_count/action_summary(primary_reason_source/primary_severity/reason_source_counts/reason_code_counts)/evaluation_summary/component_status_counts/evaluated_status_counts/staleness_evaluated/boundary/reason_source_counts/reason_summary(reason_code_counts)` 给出只读 `observe/manual_review` posture、布尔复核标志、建议动作、action count、action rollup、advisory-only 边界与 evaluated/stale/not_evaluated/fresh 分量汇总；bridge HTTP/registry/CLI watch-status 已转发 `heartbeat_stale_after_seconds`、`watermark_stale_after_seconds` 与 `reconnect_stale_after_seconds`，CLI `--view summary` 与 HTTP `watch/status?view=summary` 可只读投影 `status_summary.schema_version`、`runtime.control_state/active/watch_state/run_id/pid`、`staleness_evaluated`、`governance.boundary`、`governance.reason_count`、`governance.action_count`、`governance.reason_source_counts`、`governance.reason_summary.reason_code_counts`、有界 `governance.reason_samples` / `reason_sample_limit` / `reason_sample_truncated`、有界 `governance.action_samples` / `action_sample_limit` / `action_sample_truncated`、含 `evaluated_components` / `evaluated_count` / `stale_components` / `stale_count` / `fresh_components` / `fresh_count` / `not_evaluated_components` / `not_evaluated_count` / `component_status_counts` / `evaluated_status_counts` 的 `evaluation_summary` 和含 `primary_reason_source` / `primary_severity` / `severity_counts` / `action_name_counts` / `reason_source_counts` / `reason_code_counts` 的 action rollup；`tests/test_subscription_watch_background.py`、`tests/test_bridge_http.py`、`tests/test_bridge_registry.py`、`tests/test_api_cli.py`；OpenSpec `subscription-long-run-status-summary` / `subscription-watch-heartbeat-staleness-summary` / `subscription-watch-watermark-staleness-summary` / `subscription-watch-reconnect-staleness-summary` / `subscription-watch-governance-summary` / `subscription-governance-action-summary` / `subscription-governance-review-flag` / `subscription-governance-action-rollup` / `subscription-governance-action-primary-reason-source` / `subscription-governance-action-primary-severity` / `subscription-watch-status-summary-view` / `subscription-watch-status-http-summary-view` / `subscription-summary-staleness-evaluated-view` / `subscription-summary-runtime-identity-view` / `subscription-governance-evaluation-summary` / `subscription-summary-governance-boundary-view` / `subscription-summary-governance-reason-count-view` / `subscription-summary-governance-reason-samples-view` / `subscription-summary-governance-action-samples-view` / `subscription-governance-action-severity-summary` / `subscription-governance-reason-source-counts` / `subscription-summary-fresh-evaluation-counts` / `subscription-summary-schema-version-view` / `subscription-evaluation-component-list-counts` / `subscription-governance-action-name-counts` / `subscription-governance-action-reason-source-counts` / `subscription-governance-action-reason-code-counts` / `subscription-governance-action-count` / `subscription-evaluation-component-status-counts` / `subscription-evaluation-evaluated-status-counts` / `subscription-governance-reason-code-counts` | 当前仍是只读 long-run status projection：保留 raw `control`/`watch_status` 在 detailed payload，summary view 只复制 `status_summary.schema_version`、选定 runtime 身份标量、`governance.boundary`、从详细 reasons 派生的 `governance.reason_count`、从详细 actions 派生的 `governance.action_count`、按 reason 前缀聚合的 `governance.reason_source_counts`、按完整 advisory reason 字符串聚合的 `governance.reason_summary.reason_code_counts`、最多 3 个代表性 `governance.reason_samples`、最多 3 个去掉 description 的 compact `governance.action_samples`、primary action reason source、primary severity 和 governance rollup，不暴露 raw payload、完整 `governance.reasons` 或完整 `governance.actions`；默认 `staleness=not_evaluated`、`governance.decision=observe`、`requires_manual_review=false`、`staleness_evaluated=false`、`action_count=0`、`action_summary.count=0`、`action_summary.primary_reason_source=null`、`action_summary.primary_severity="none"`、`evaluated_count=0`、`stale_count=0`、`fresh_count=0` 且 `severity_counts={}` / `action_name_counts={}` / `action_summary.reason_source_counts={}` / `action_summary.reason_code_counts={}` / `reason_code_counts={}` / `evaluated_status_counts={}`，`evaluation_summary` 只标注未评估分量、已评估分量、stale 分量、已评估 fresh 分量、分量状态计数摘要和仅已显式评估分量状态分布，`evaluated_status_counts` 排除 `not_evaluated`；显式 stale 诊断或 reconnect/degraded/failed 只会生成 `manual_review` 理由、advisory actions、advisory-only boundary、reason count、action count、reason source counts、reason code counts、action reason source counts、action reason code counts、reason samples、action samples、action/evaluation rollup、primary action reason source、primary severity、fresh/evaluated/stale/not-evaluated 分量摘要、component status counts、evaluated status counts、action severity/action-name 聚合、runtime identity summary 投影和复核标志，HTTP/CLI summary view 只是 opt-in 响应投影，不改 reconnect/backoff 调度，不改变 start/stop/list/events/logs 或 SSE/event-stream contract；`status_summary.schema_version` 只是摘要结构标识，不是能力开关、健康保证或 readiness 证明；`evaluated_components` / `evaluated_count` / `stale_components` / `stale_count` / `fresh_components` / `fresh_count` / `not_evaluated_components` / `not_evaluated_count` / `component_status_counts` / `evaluated_status_counts` 不是健康保证或自动恢复条件，`reason_source_counts` 和 `reason_code_counts` 不是完整 reasons 清单，`action_count` 不是执行许可或动作队列长度保证，`action_summary.primary_severity` 只是第一条 advisory action 的 severity，与兼容字段 `severity` 同口径，不是执行等级、自动升级策略或自动化治理，`severity_counts` 不是执行等级、自动升级策略或自动化治理，`action_name_counts`、`action_summary.primary_reason_source`、`action_summary.reason_source_counts` 和 `action_summary.reason_code_counts` 不是完整 actions 清单、执行指令或治理策略。 |
| E-10 | 更多 catalog 预览/发现能力 | `[已实现]` | `tdxquant/cli.py` 支持 `catalog preview` 非执行预览、list discovery metadata、summary-view 输出约束，并让 `catalog plan` / `catalog preview` 在 entry/bundle 目标上返回 `provenance` 与 `constraints`；`tests/test_api_cli.py` 覆盖 label/bundle discovery、entry preview、bundle preview、reduced summary payload、entry plan provenance 与 bundle preview constraints；OpenSpec `catalog-discovery-preview-hardening` / `catalog-plan-provenance-summary` | 当前是 catalog CLI discovery/preview/plan 的只读输出硬化；`provenance` 仅声明 catalog/bundle 文件来源和目标，`constraints` 仅声明非执行边界；不改变 `runtime/command-catalog.json` / `runtime/command-bundles.json` schema，不新增业务 contract，也不改变 `catalog run` 执行语义。 |
| E-11 | 更多 task/report 组合入口 | `[部分实现]` | `runtime/command-bundles.json` 约 `167` 个 bundle，其中约 `113` 个组合 `task` 与 `report` step；`catalog validate --kind all` / `catalog validate --kind bundle --label followup` 非执行校验固定 entry/bundle 解析并输出 `entry_source_counts`、`entry_label_counts`、`bundle_step_count`、`bundle_step_source_counts`、`bundle_step_name_counts`、`bundle_step_entry_counts`、`bundle_step_source_name_counts`、`bundle_step_option_key_counts`、`bundle_step_source_option_key_counts`、`bundle_label_counts`、`task_report_bundle_count`、有界 `task_report_bundle_samples`、`task_report_bundle_step_count`、`task_report_bundle_step_source_counts`、`task_report_bundle_step_name_counts`、`task_report_bundle_step_source_name_counts`、`task_report_bundle_step_entry_counts`、`task_report_bundle_step_option_key_counts`、`task_report_bundle_step_source_option_key_counts`、`task_report_bundle_label_counts`、`submit_once_bundle_label_counts`、`pingan_bundle_label_counts`、`submit_once_bundle_step_source_counts`、`pingan_bundle_step_source_counts`、`submit_once_bundle_step_name_counts`、`pingan_bundle_step_name_counts`、`submit_once_bundle_step_source_name_counts`、`pingan_bundle_step_source_name_counts`、`submit_once_bundle_step_entry_counts`、`submit_once_bundle_step_option_key_counts`、`pingan_bundle_step_entry_counts` 与 `pingan_bundle_step_option_key_counts`；summary view 同步投影这些只读计数，并为 submit-once/pingan bundle 子集输出 label/step-source/step-name/source-name/entry/option-key counts 与有界样本；`catalog plan/preview --bundle ... --view summary` 对选中的 bundle step 投影 `step_source_counts`；代表入口包括 `confirm-complete-review`、`submit-ready-complete-review`、`guarded-buy-complete-review`、`buy-pingan-complete-review`、`sell-pingan-complete-review`、`confirm-current-pingan-complete-review`、`buy-submit-once-pingan-complete-review`、`sell-submit-once-pingan-complete-review` 及异常/拒绝/失败 follow-up bundle；`tests/test_api_cli.py` 覆盖 follow-up label discovery、catalog plan、catalog validate detailed/summary、entry source/label counts、bundle step count/source/name/entry/source-name/option-key/source-option-key counts、task/report sample/source/name/source-name/entry/option-key/source-option-key/label counts、submit-once/pingan label/step-source/step-name/source-name/entry/option-key counts/samples、PingAn alias 和 buy/sell/confirm_current/submit_once follow-up bundle 非执行解析；OpenSpec `task-report-combo-entry-registry` / `command-catalog-validate-registry` / `command-catalog-validate-summary-view` / `command-catalog-validate-task-report-samples` / `command-catalog-validate-sample-truncation` / `command-catalog-validate-submit-once-samples` / `command-catalog-validate-pingan-samples` / `command-catalog-plan-step-source-summary-view` / `catalog-validate-task-report-source-summary` / `catalog-validate-task-report-label-summary` / `catalog-validate-task-report-step-count` / `catalog-validate-task-report-step-name-counts` / `catalog-validate-task-report-step-source-name-counts` / `catalog-validate-task-report-step-entry-counts` / `command-catalog-validate-step-option-key-counts` / `catalog-validate-bundle-step-source-option-key-counts` / `catalog-validate-task-report-step-source-option-key-counts` / `command-catalog-validate-entry-label-counts` / `command-catalog-validate-entry-source-counts` / `catalog-validate-bundle-step-count` / `catalog-validate-bundle-label-counts` / `catalog-validate-bundle-source-counts` / `catalog-validate-bundle-step-name-counts` / `catalog-validate-bundle-step-entry-counts` / `catalog-validate-bundle-step-source-name-counts` / `catalog-validate-submit-pingan-label-counts` / `catalog-validate-submit-pingan-step-source-counts` / `catalog-validate-submit-pingan-step-name-counts` / `catalog-validate-submit-pingan-step-source-name-counts` / `catalog-validate-submit-pingan-step-entry-counts` / `catalog-validate-submit-pingan-step-option-key-counts` | 当前是固定 runtime JSON preset/bundle 组合入口，不是任意 workflow builder；`catalog validate` 只是结构解析校验、覆盖计数、有界样本投影、entry source/label 聚合计数、bundle step 总数、selected bundle step source/name/entry/source-name/option-key/source-option-key/label 聚合计数、task/report bundle step 总数、task/report step source/name/source-name/entry/option-key/source-option-key 聚合计数、task/report label 聚合计数、submit-once/PingAn bundle label 聚合计数、submit-once/PingAn bundle step source 聚合计数、submit-once/PingAn bundle step name 聚合计数、submit-once/PingAn bundle step source-name 聚合计数、submit-once/PingAn bundle step entry 聚合计数和 submit-once/PingAn bundle step option-key 聚合计数，不执行 task/report/trade/bundle step；`entry_source_counts` 只对已解析 selected entry 的 source 聚合，`entry_label_counts` 只对已解析 selected entry 的 labels 聚合，不列出完整 entry 明细，也不是执行覆盖、可用性证明或 readiness 证明；`bundle_step_count`、`bundle_step_source_counts`、`bundle_step_name_counts`、`bundle_step_entry_counts`、`bundle_step_source_name_counts`、`bundle_step_option_key_counts`、`bundle_step_source_option_key_counts`、`bundle_label_counts` 都只对已解析 selected bundle 聚合，不列出完整 bundle/step/entry/source-name/option/value/label 明细，也不是执行证据、可用性证明或完整 workflow builder；`bundle_step_source_option_key_counts` 只统计 `source:option_key` 分布，不暴露 option value、不校验 option 语义；`task_report_bundle_step_source_counts`、`task_report_bundle_step_name_counts`、`task_report_bundle_step_source_name_counts`、`task_report_bundle_step_entry_counts`、`task_report_bundle_step_option_key_counts` 与 `task_report_bundle_step_source_option_key_counts` 只对包含 task+report 的 selected bundle 子集聚合，不代表完整步骤明细、完整 entry/options 清单或执行链路；`task_report_bundle_step_source_option_key_counts` 只统计 task+report bundle 子集的 `source:option_key` 分布，不暴露 option value、不校验 option 语义；`submit_once_bundle_label_counts` 与 `pingan_bundle_label_counts` 只对 selected resolved submit-once/PingAn bundle 子集的 bundle labels 聚合，`submit_once_bundle_step_source_counts` 与 `pingan_bundle_step_source_counts` 只对这些子集的 resolved step source 聚合，`submit_once_bundle_step_name_counts` 与 `pingan_bundle_step_name_counts` 只对这些子集的 resolved step name 聚合，`submit_once_bundle_step_source_name_counts` 与 `pingan_bundle_step_source_name_counts` 只对这些子集的 resolved step `source:name` 聚合，`submit_once_bundle_step_entry_counts` 与 `pingan_bundle_step_entry_counts` 只对这些子集的 resolved step entry 聚合，`submit_once_bundle_step_option_key_counts` 与 `pingan_bundle_step_option_key_counts` 只对这些子集的 resolved step option key 聚合，不暴露 option value、不校验 option 语义、不代表执行覆盖、执行链路、broker readiness、交易安全证明或完整 step manifest；`task_report_bundle_samples`、`submit_once_bundle_samples` 与 `pingan_bundle_samples` 只是最多 5 个确定性样本，`*_sample_limit` / `*_sample_truncated` 只说明样本上限和是否被截断；`--view summary` 只是 opt-in reduced projection，不改变默认 detailed validation payload；plan/preview 的 `step_source_counts` 只按已解析 selected steps 的 catalog source 计数；bundle 仍通过既有 `task` / `report` catalog entry 分发，不新增底层交易、报表或聚合语义，普通 buy/sell/confirm_current/submit_once follow-up 真实执行仍需显式交易安全前置条件。 |
| E-12 | trade_audit 高阶聚合 | `[已实现]` | `tdxquant/trade_audit_index.py` 提供 audit index cache、跨 `trade_audit` / submission ledger / task ledger 只读查询，以及 `aggregation.by_status/by_method/by_broker/by_broker_method_status` 计数聚合；`TdxTaskManager.trade_audit_cross_ledger_query` 与 `task trade-audit-cross-ledger-query` 提供入口；`tests/test_trade_audit_index.py` 和 `tests/test_api_cli.py` 覆盖缓存、join、损坏文件容错、limit 前聚合、unknown 维度和 CLI parse；OpenSpec `trade-audit-index-and-cross-ledger-query` / `trade-audit-index-aggregation-summary` | 当前是只读 exact-key 诊断查询和计数聚合，cache 是可重建派生 artifact；不改写历史 audit/ledger，不做成交金额/数量/价格/PnL/执行质量聚合，也不代表 live broker/provider 新能力。 |
| E-13 | 桌面交易扩展 broker capability 边界 | `[已实现]` | `tdxquant/trade/extended_capabilities.py` 提供 PingAn desktop funds/positions/cancel/native-push capability probe；`trade broker-capabilities`、`trade run --preset broker-capabilities-default` 与 `catalog plan --entry broker-capabilities` 提供非执行/诊断入口；`runtime/trade-presets.json` 与 `runtime/command-catalog.json` 登记稳定 preset/catalog entry；`docs/trading/desktop_trade_extended_broker_capabilities_risk.md` 记录独立风险边界；`tests/test_trade_extended_capabilities.py` 与 `tests/test_api_cli.py` 覆盖 payload、manager、CLI、preset 与 catalog plan；OpenSpec `desktop-trade-extended-broker-capabilities` / `desktop-broker-capability-catalog-entry` | 当前是只读/非执行诊断边界：资金和持仓仅报告 capability metadata，撤单仅做 broker-state-mutating 分级，broker-native push 仅报告 feasibility boundary；不执行资金/持仓查询、不提交撤单、不打开 broker-native push，不并入 query API 或默认交易主线；catalog plan 仅解析，catalog run/trade run 也只进入既有 diagnostic probe。 |
| E-14 | tqcenter 官方 API 全量桥接 (17 functions) | `[已实现]` | `tdxquant/api/bridge.py` 新增 17 个 `run_tdx_*` 桥接函数（Group A: `send_message`/`send_file`/`send_bt_data`; Group B: `get_relation`/`gb_info_by_date`/`get_pricevol`/`get_trackzs_etf_info`/`formula_get_all`/`formula_get_info`/`print_to_tdx`/`exec_to_tdx`; Group C: `stock_account`/`order_stock`/`query_stock_orders`/`query_stock_positions`/`cancel_order_stock`/`query_stock_asset`）；`tdxquant/api/runtime.py` 新增 `send_message`/`send_file`/`send_bt_data`/`print_to_tdx`/`exec_to_tdx`；`tdxquant/api/market.py` 新增 `get_pricevol`/`get_trackzs_etf_info`；`tdxquant/api/meta.py` 新增 `get_relation`/`gb_info_by_date`；`tdxquant/api/formula.py` 新增 `get_all`/`get_info`；新文件 `tdxquant/api/trade_api.py` 提供 `TradeApi` 类含 6 个交易方法；`tdxquant/api/manager.py` 新增 `_TradeApiManagerProxy` 与对 `_RuntimeManagerProxy`/`_MarketManagerProxy`/`_MetaManagerProxy`/`_FormulaManagerProxy` 的方法扩展；`TdxApiManager` 新增 `trade` 属性；`tdxquant/provider_discovery.py` 新增 17 个 `_capability(...)` 条目（总计 72 capabilities）；`tdxquant/cli.py` 新增 17 个 parser block 和 17 个 dispatch block；`tests/test_api_cli.py` 全部 469 tests passed 无回归 | Group A（3 函数）桥接已存在于 `tqcenter.py` 的 DLL 方法，可直接调用；Group B（8 函数）使用 `_require_tq_method` 前向兼容策略，在 tqcenter 实现前会抛出 RuntimeError；Group C（6 交易函数）使用相同前向兼容策略，`order_stock` 已在 `tqcenter.py:2181` 存在 DLL 入口但其他 5 个尚不存在；原 PingAn 桌面自动化交易主线（Section D）保持不变，作为独立平行路径；Group B/C 函数在 tqcenter 真正实现前需要配套 DLL 支持。 |

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：`status_summary.governance.evaluation_summary.primary_fresh_component` 从既有 `fresh_components` 顺序派生首个 fresh 分量，并通过 detailed status、HTTP summary view 与 CLI summary view 投影，证据为 `tdxquant/subscription_watch_background.py`、`tests/test_subscription_watch_background.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-evaluation-primary-fresh-component`；该字段只是 compact advisory hint，不暴露 raw control/watch payload 或完整 governance arrays，不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：`status_summary.governance.evaluation_summary.primary_evaluated_component` 从既有 `evaluated_components` 顺序派生首个已评估分量，并通过 detailed status、HTTP summary view 与 CLI summary view 投影，证据为 `tdxquant/subscription_watch_background.py`、`tests/test_subscription_watch_background.py` 与 OpenSpec `subscription-evaluation-primary-evaluated-component`；该字段只是 compact advisory identity hint，不暴露 raw control/watch payload 或完整 governance arrays，不改变 staleness 判定，不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：`status_summary.governance.evaluation_summary.primary_not_evaluated_component` 从既有 `not_evaluated_components` 顺序派生首个未评估分量，并通过 detailed status、HTTP summary view 与 CLI summary view 投影，证据为 `tdxquant/subscription_watch_background.py`、`tests/test_subscription_watch_background.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-evaluation-primary-not-evaluated-component`；该字段只是 compact advisory hint，不暴露 raw control/watch payload 或完整 governance arrays，不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：`status_summary.governance.evaluation_summary.primary_stale_component` 从既有 `stale_components` 顺序派生首个 stale 分量，并通过 detailed status、HTTP summary view 与 CLI summary view 投影，证据为 `tdxquant/subscription_watch_background.py`、`tests/test_subscription_watch_background.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-evaluation-primary-stale-component`；该字段只是 compact advisory hint，不暴露 raw control/watch payload 或完整 governance arrays，不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：`status_summary.governance.action_summary.primary_severity` 从首个 advisory action 的 `severity` 派生，并通过 detailed status、HTTP summary view 与 CLI summary view 投影，证据为 `tdxquant/subscription_watch_background.py`、`tests/test_subscription_watch_background.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-governance-action-primary-severity`；该字段只是 compact advisory hint，与兼容字段 `severity` 同口径，不暴露 raw control/watch payload 或完整 governance arrays，不代表执行等级、自动升级策略或治理策略，不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：`status_summary.governance.action_summary.severity_key_count` / `action_name_key_count` / `reason_source_key_count` / `reason_code_key_count` 从既有 `severity_counts` / `action_name_counts` / `reason_source_counts` / `reason_code_counts` map 派生不同 key 数量，并通过 detailed status、HTTP summary view 与 CLI summary view 保留，证据为 `tdxquant/subscription_watch_background.py`、`tests/test_subscription_watch_background.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-action-summary-key-counts`；这些字段只是 action summary count map 的只读 cardinality hint，不替代 `governance.action_count`、`action_summary.count` 或各 count map，不暴露完整 actions，不代表执行队列、升级策略、自动化治理、健康/readiness、PID liveness 或 process ownership，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：`status_summary.governance.reason_summary.primary_reason_source` 从既有 `primary_source` 同值派生，并通过 detailed status、HTTP summary view 与 CLI summary view 投影，证据为 `tdxquant/subscription_watch_background.py`、`tests/test_subscription_watch_background.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-governance-reason-primary-source-alias`；该字段只是 reason summary 的命名兼容别名，不移除 `primary_source`，不暴露 raw control/watch payload 或完整 governance reasons，不代表治理策略、执行指令或自动化条件，不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：`status_summary.governance.reason_summary.source_key_count` 与 `reason_code_key_count` 从既有 `source_counts` / `reason_code_counts` map 派生不同 reason source/code key 数量，并通过 detailed status、HTTP summary view 与 CLI summary view 保留，证据为 `tdxquant/subscription_watch_background.py`、`tests/test_subscription_watch_background.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-reason-summary-key-counts`；两个字段只是 reason summary count map 的只读 cardinality hint，不替代 `governance.reason_count`、`source_counts` 或 `reason_code_counts`，不暴露完整 reasons，不证明健康、readiness、PID liveness、process ownership 或治理策略，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：`status_summary.governance.reason_source_key_count` 从顶层 `reason_source_counts` map 派生不同 reason source key 数量，并通过 detailed status、HTTP summary view 与 CLI summary view 投影，证据为 `tdxquant/subscription_watch_background.py`、`tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_subscription_watch_background.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-governance-reason-source-key-count`；该字段只是顶层 reason source count map 的只读 cardinality hint，不替代 `governance.reason_count`、`reason_source_counts` 或 `reason_summary.source_key_count`，不暴露完整 reasons，不证明健康、readiness、PID liveness、process ownership、升级策略或治理策略，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：HTTP/CLI `watch-status --view summary` 新增只读 `status_summary.governance.reason_sample_count` 与 `status_summary.governance.action_sample_count`，证据为 `tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-summary-governance-sample-counts`；两个字段只从已投影的有界 `reason_samples` / `action_samples` 可见数组派生，不替代完整 `reason_count` / `action_count`，不暴露 raw control/watch payload、完整 `governance.reasons` 或完整 `governance.actions`，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：HTTP/CLI `watch-status --view summary` 新增只读 `status_summary.governance.reason_sample_hidden_count` 与 `status_summary.governance.action_sample_hidden_count`，证据为 `tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-summary-governance-sample-hidden-counts`；两个字段只从完整 `reason_count` / `action_count` 和已投影的有界 `reason_sample_count` / `action_sample_count` 派生非负差值，用来说明 compact summary 中未展示的样本数量，不替代完整 reasons/actions 清单，不暴露 raw control/watch payload、完整 `governance.reasons` 或完整 `governance.actions`，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：HTTP/CLI `watch-status --view summary` 新增只读 `status_summary.governance.sample_summary`，集中投影既有 `reason_count` / `reason_sample_count` / `reason_sample_hidden_count` / `reason_sample_limit` / `reason_sample_truncated` 与 `action_count` / `action_sample_count` / `action_sample_hidden_count` / `action_sample_limit` / `action_sample_truncated`，证据为 `tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-governance-sample-summary`；该对象只汇总 compact summary 的样本元数据，不替代既有 sibling 字段，不暴露 raw control/watch payload、完整 `governance.reasons` 或完整 `governance.actions`，不代表治理策略、执行队列、升级策略、健康/readiness、PID liveness 或 process ownership，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：`status_summary.governance.evaluation_summary.component_status_key_count` 与 `evaluated_status_key_count` 从既有 `component_status_counts` / `evaluated_status_counts` map 派生不同 status key 数量，并通过 detailed status、HTTP summary view 与 CLI summary view 保留，证据为 `tdxquant/subscription_watch_background.py`、`tests/test_subscription_watch_background.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-evaluation-status-key-counts`；两个字段只是 evaluation status map 的只读 cardinality hint，不替代 component lists/counts 或 status-count maps，不改变 staleness 判断，不证明健康、readiness、PID liveness 或 process ownership，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：HTTP/CLI `watch-status --view summary` 新增只读 `runtime.state_match`，证据为 `tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-summary-runtime-state-match`；该字段只在 `control.state` 与 `watch_status.state` 同时存在时比较二者是否相等，不替代 `overall_status`、staleness 诊断或 governance decision，不证明健康/readiness，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：HTTP/CLI `watch-status --view summary` 新增只读 `runtime.run_id_source`，证据为 `tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-summary-runtime-run-id-source`；该字段只说明 summary 中 `runtime.run_id` 来自 `watch_status.run_id` 还是 fallback `control.run_id`，不证明 run ownership、健康/readiness 或新旧程度，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：HTTP/CLI `watch-status --view summary` 新增只读 `runtime.run_id_match`，证据为 `tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-summary-runtime-run-id-match`；该字段只在 `control.run_id` 与 `watch_status.run_id` 同时存在时比较二者是否相等，不改变 `runtime.run_id` source precedence，不证明 run ownership、健康/readiness、新旧程度或生命周期状态，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：HTTP/CLI `watch-status --view summary` 新增只读 `runtime.pid_source`，证据为 `tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-summary-runtime-pid-source`；该字段只在 summary 已从 `control.pid` 投影 `runtime.pid` 时标注来源为 `control`，不新增 PID fallback、不证明 PID 存活、process ownership、健康/readiness 或生命周期状态，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：HTTP/CLI `watch-status --view summary` 新增只读 `runtime.identity_summary`，集中投影既有 control/watch state、`state_match`、run_id presence/source/match 与 pid presence/source，证据为 `tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-summary-runtime-identity-summary`；该对象只汇总 compact runtime identity metadata，不替代既有 sibling 字段，不暴露 raw control/watch payload、event-stream data 或 lifecycle control，不证明 PID 存活、run ownership、新旧程度、健康/readiness 或 process ownership，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：HTTP/CLI `watch-status --view summary` 新增只读 `governance.decision_summary`，集中投影既有 governance decision、manual-review flag、staleness-evaluated flag、reason/action count、primary reason source 与 primary severity，证据为 `tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-governance-decision-summary`；该对象只汇总 advisory governance decision metadata，不替代既有 sibling 字段，不暴露 raw control/watch payload、完整 reasons/actions、event-stream data 或 lifecycle control，不证明健康/readiness、PID 存活、run ownership 或生产治理策略，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：HTTP/CLI `watch-status --view summary` 的只读 `governance.decision_summary` 新增 `has_reasons` / `has_actions` presence flags，分别从已投影的 `reason_count` / `action_count` 派生，证据为 `tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-decision-presence-flags`；这两个字段只是 compact summary 的 count>0 convenience flags，不替代既有 sibling count 字段，不暴露 raw control/watch payload、完整 reasons/actions、event-stream data 或 lifecycle control，不证明健康/readiness、PID 存活、run ownership 或生产治理策略，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：HTTP/CLI `watch-status --view summary` 的只读 `governance.decision_summary` 新增 `primary_reason`，证据为 `tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-decision-summary-primary-reason`；该字段只从既有 `governance.reason_summary.primary_reason` 投影到 compact decision summary，不替代 detailed `reason_summary`，不暴露 raw control/watch payload、完整 reasons/actions、event-stream data 或 lifecycle control，不代表执行指令、自动治理策略、健康/readiness、PID 存活或 run ownership，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：HTTP/CLI `watch-status --view summary` 的只读 `governance.decision_summary` 新增 `reason_source_key_count` 与 `reason_code_key_count`，证据为 `tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-decision-summary-reason-key-counts`；这些字段只从既有 `governance.reason_summary.source_key_count` / `reason_code_key_count` 投影到 compact decision summary，用于 summary-level source/code diversity 校验，不替代 detailed `reason_summary`，不暴露 raw control/watch payload、完整 reasons/actions、event-stream data 或 lifecycle control，不代表执行指令、自动治理策略、健康/readiness、PID 存活或 run ownership，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：HTTP/CLI `watch-status --view summary` 的只读 `governance.decision_summary` 新增 `primary_action` 与 `primary_action_reason`，证据为 `tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-decision-summary-primary-action`；这些字段只从既有 `governance.action_summary.primary_action` / `primary_reason` 投影到 compact decision summary，不替代 detailed `action_summary`，不暴露 raw control/watch payload、完整 reasons/actions、event-stream data 或 lifecycle control，不代表执行指令、自动治理策略、健康/readiness、PID 存活或 run ownership，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：HTTP/CLI `watch-status --view summary` 的只读 `governance.decision_summary` 新增 `primary_action_reason_source`，证据为 `tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-decision-summary-primary-action-source`；该字段只从既有 `governance.action_summary.primary_reason_source` 投影到 compact decision summary，用来区别已有 `primary_reason_source` 的 reason-summary 来源，不替代 detailed `action_summary`，不暴露 raw control/watch payload、完整 reasons/actions、event-stream data 或 lifecycle control，不代表执行指令、自动治理策略、健康/readiness、PID 存活或 run ownership，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：HTTP/CLI `watch-status --view summary` 新增只读 `governance.evaluation_rollup`，集中投影既有 `evaluation_summary` 的 evaluated/stale/fresh/not-evaluated counts、primary stale/fresh/not-evaluated component 与保守 boolean hints，证据为 `tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-governance-evaluation-rollup`；该对象只汇总 advisory staleness evaluation metadata，不替代既有 `evaluation_summary` 或 sibling 字段，不暴露 raw control/watch payload、完整 reasons/actions、event-stream data 或 lifecycle control，不证明健康/readiness、PID 存活、run ownership 或生产治理策略，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：HTTP/CLI `watch-status --view summary` 的只读 `governance.evaluation_rollup` 补充 `has_not_evaluated_component`、`component_status_key_count` 与 `evaluated_status_key_count`，证据为 `tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-governance-evaluation-rollup-completeness`；这些字段只从既有 `evaluation_summary.not_evaluated_count`、`component_status_key_count`、`evaluated_status_key_count` 投影到 compact summary，不重新计算 staleness，不替代 detailed `evaluation_summary`，不暴露 raw control/watch payload、完整 reasons/actions、event-stream data 或 lifecycle control，不证明健康/readiness、PID 存活、run ownership 或生产治理策略，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：HTTP/CLI `watch-status --view summary` 的只读 `governance.evaluation_rollup` 补充 `primary_evaluated_component` 与 `has_evaluated_component`，证据为 `tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-governance-evaluation-rollup-evaluated-fields`；这些字段只从既有 `evaluation_summary.primary_evaluated_component` 与 `evaluated_count` 投影到 compact summary，不重新计算 staleness，不替代 detailed `evaluation_summary`，不暴露 raw control/watch payload、完整 reasons/actions、event-stream data 或 lifecycle control，不证明健康/readiness、PID 存活、run ownership 或生产治理策略，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：direct status、HTTP/CLI `watch-status --view summary` 新增只读 `governance.reconnect_rollup`，集中投影既有 `reconnect.staleness`、`reconnect_count`、`consecutive_reconnect_failures`、last-error presence、next-reconnect presence、`age_source` 与 `stale_after_seconds`，证据为 `tdxquant/subscription_watch_background.py`、`tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_subscription_watch_background.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-governance-reconnect-rollup`；该对象只汇总 reconnect/backoff 相关诊断信号，不读取 statefile/lock、不证明进程 ownership、supervisor 存活、真实 backoff 调度或 restart readiness，不暴露 raw control/watch payload、完整 reasons/actions、event-stream data 或 lifecycle control，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：direct status、HTTP/CLI `watch-status --view summary` 新增只读 `status_summary.control_rollup`，集中投影 reconciled `control.state`、`active`、run_id/pid presence、`reason` 与 stale/startup-failure reason flags，证据为 `tdxquant/subscription_watch_background.py`、`tdxquant/bridge_http.py`、`tdxquant/cli.py`、`tests/test_subscription_watch_background.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-control-rollup-summary`；该对象只汇总 control payload 中已有的 statefile-derived identity/reason signals，不读取 lock、不证明 PID liveness、process ownership、supervisor ownership、readiness、statefile 新旧程度或 lifecycle control，不暴露 raw control/watch payload、完整 reasons/actions、event-stream data，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：direct status、HTTP/CLI `watch-status --view summary` 的 `governance.evaluation_summary` 新增只读 `has_stale_component`，证据为 `tdxquant/subscription_watch_background.py`、`tests/test_subscription_watch_background.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-evaluation-has-stale-component`；该字段只从既有 `stale_components` / `stale_count` 派生是否存在 stale 分量，不替代 `stale_components`、`primary_stale_component`、`component_status_counts`、`evaluation_rollup`、manual-review decision 或 action/reason 明细，不暴露 raw control/watch payload、完整 reasons/actions、HTTP/SSE/event-stream data 或 lifecycle control，不证明健康/readiness、PID 存活、run ownership 或生产治理策略，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：direct status、HTTP/CLI `watch-status --view summary` 的 `governance.evaluation_summary` 新增只读 `has_not_evaluated_component`，证据为 `tdxquant/subscription_watch_background.py`、`tests/test_subscription_watch_background.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-evaluation-has-not-evaluated-component`；该字段只从既有 `not_evaluated_components` / `not_evaluated_count` 派生是否存在未评估分量，不替代 `not_evaluated_components`、`primary_not_evaluated_component`、`component_status_counts`、`evaluation_rollup`、manual-review decision 或 action/reason 明细，不暴露 raw control/watch payload、完整 reasons/actions、HTTP/SSE/event-stream data 或 lifecycle control，不证明健康/readiness、PID 存活、run ownership、评估完整性或生产治理策略，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：direct status、HTTP/CLI `watch-status --view summary` 的 `governance.evaluation_summary` 新增只读 `has_fresh_component`，证据为 `tdxquant/subscription_watch_background.py`、`tests/test_subscription_watch_background.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-evaluation-has-fresh-component`；该字段只从既有 `fresh_components` / `fresh_count` 派生是否存在 fresh 分量，不替代 `fresh_components`、`primary_fresh_component`、`component_status_counts`、`evaluation_rollup`、manual-review decision 或 action/reason 明细，不暴露 raw control/watch payload、完整 reasons/actions、HTTP/SSE/event-stream data 或 lifecycle control，不证明健康/readiness、PID 存活、run ownership、评估完整性或生产治理策略，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> B-16/E-09 补充登记（状态仍为 `[部分实现]`）：direct status、HTTP/CLI `watch-status --view summary` 的 `governance.evaluation_summary` 新增只读 `all_components_evaluated`，证据为 `tdxquant/subscription_watch_background.py`、`tests/test_subscription_watch_background.py`、`tests/test_bridge_http.py`、`tests/test_api_cli.py` 与 OpenSpec `subscription-evaluation-all-components-evaluated`；该字段只从既有 `not_evaluated_components` / `not_evaluated_count` 派生是否所有治理分量已进入 fresh/stale 评估，不替代 `not_evaluated_components`、`has_not_evaluated_component`、`component_status_counts`、`evaluation_rollup`、manual-review decision 或 action/reason 明细，不暴露 raw control/watch payload、完整 reasons/actions、HTTP/SSE/event-stream data 或 lifecycle control，不证明健康/readiness、PID 存活、run ownership、评估质量或生产治理策略，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.primary_requested_probe`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-primary-requested-probe`；该字段只从既有 `requested` 目标列表派生首个已请求 probe，`null` 只表示当前没有 requested probe，不请求额外 probe、不替代 `requested_count` / `request_coverage_status` / 单项 probe 证据，不证明健康状态、readiness 或 endpoint 覆盖，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.status_key_count` / `requested_status_key_count` / `failed_status_key_count`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py` 与 OpenSpec `provider-replay-probe-status-key-counts`；这些字段只从既有 `status_counts` / `requested_status_counts` / `failed_status_counts` map 派生不同 status key 数量，不请求额外 probe、不替代 `requested_count` / `failed_count` / `request_coverage_status` / 单项 probe 证据，不证明健康状态、readiness、endpoint 覆盖或 live provider availability，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.requested_reachability_key_count` / `healthy_reachability_key_count` / `failed_reachability_key_count`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py` 与 OpenSpec `provider-replay-probe-reachability-key-counts`；这些字段只从既有 `requested_reachability_counts` / `healthy_reachability_counts` / `failed_reachability_counts` map 派生不同 reachability key 数量，不请求额外 probe、不替代 `requested_count` / `healthy_count` / `failed_count` / `request_coverage_status` / 单项 probe 证据，不证明健康状态、readiness、endpoint 覆盖、failure coverage 或 live provider availability，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.requested_http_status_key_count` / `healthy_http_status_key_count` / `failed_http_status_key_count`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py` 与 OpenSpec `provider-replay-probe-http-status-key-counts`；这些字段只从既有 `requested_http_status_counts` / `healthy_http_status_counts` / `failed_http_status_counts` map 派生不同 HTTP status key 数量，不请求额外 probe、不替代 `requested_count` / `healthy_count` / `failed_count` / `request_coverage_status` / 单项 probe 证据，不证明健康状态、readiness、HTTP endpoint 覆盖、failure coverage 或 live provider availability，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.error_code_key_count` / `failed_error_code_key_count` / `error_sample_status_key_count` / `error_sample_probe_key_count`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py` 与 OpenSpec `provider-replay-probe-error-key-counts`；这些字段只从既有 `error_code_counts` / `failed_error_code_counts` / `error_sample_status_counts` / `error_sample_probe_counts` map 派生不同 error/status/probe key 数量，不请求额外 probe、不暴露完整 error 或 probe payload、不替代 `error_sample_count` / `error_sample_truncated` / 单项 probe 证据，不证明健康状态、readiness、failure coverage 或 live provider availability，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.error_sample_http_status_counts` / `error_sample_http_status_key_count`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-error-sample-http-status-counts`；这些字段只统计进入紧凑错误样本候选集合且带整数 `http_status` 的 HTTP status 分布和不同 key 数量，独立于被截断的 `error_samples` 列表，不替代 `requested_http_status_counts` / `healthy_http_status_counts` / `failed_http_status_counts` / 单项 probe 证据，不暴露完整 error 或 probe payload，不证明健康状态、readiness、HTTP endpoint 覆盖、failure coverage 或 live provider availability，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.error_sample_reachability_counts` / `error_sample_reachability_key_count`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-error-sample-reachability-counts`；这些字段只统计进入紧凑错误样本候选集合的 `reachable` / `unreachable` / `unknown` 分布和不同 key 数量，独立于被截断的 `error_samples` 列表，不替代 `requested_reachability_counts` / `healthy_reachability_counts` / `failed_reachability_counts` / 单项 probe 证据，不暴露完整 error 或 probe payload，不证明健康状态、readiness、endpoint 覆盖、failure coverage 或 live provider availability，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.error_sample_probe_counts`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-error-sample-probe-counts`；该字段只统计进入紧凑错误样本候选集合的 probe key 分布，并独立于被截断的 `error_samples` 列表，不暴露完整 probe payload，不证明失败覆盖/健康状态，也不新增 probe 端点、socket 启动、provider mutation 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.request_coverage_status`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-request-coverage-status`；该字段只从既有 `requested_count/total_count` 派生 `none` / `partial` / `complete` 请求覆盖状态，不请求额外 probe、不证明健康状态或 endpoint 覆盖，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.request_summary`，集中投影既有 `request_coverage_status`、total/requested/not-requested count、healthy/failed/unhealthy count 及 primary requested/not-requested probe，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-request-summary`；该对象只汇总固定 probe 请求覆盖 metadata，不替代既有 sibling 字段，不暴露完整 probe payload、error samples、endpoint response body 或 daemon control，不请求额外 probe、不证明健康状态、readiness、endpoint 覆盖或 live provider availability，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.health_summary`，集中投影既有 probe summary status、healthy/failed/unhealthy count、status key count 及 primary healthy/failed/unhealthy probe，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-health-summary`；该对象只汇总固定 probe 健康态 metadata，不替代既有 sibling 字段，不暴露完整 probe payload、error samples、endpoint response body 或 daemon control，不请求额外 probe、不证明 readiness、endpoint 覆盖、failure coverage 或 live provider availability，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 在只读 `runtime.probe_summary.health_summary` 内新增 `has_healthy_probe` / `has_failed_probe` / `has_unhealthy_probe`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-health-summary-presence-flags`；这些字段只从既有 normalized probe rollup 的 healthy/failed/unhealthy 列表或计数派生，并与顶层 sibling presence 字段保持一致，不替代 count map、primary probe、单项 probe payload 或 `outcome_summary`，不请求额外 probe、不改变健康分类、不证明 readiness、endpoint 覆盖、failure coverage、broker readiness、live provider availability 或生产 daemon control，也不新增 socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.outcome_summary`，集中投影既有 probe summary status、request coverage、probe counts、all-requested/failed/unhealthy booleans、primary problem probe 与 primary error-sample hints，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-outcome-summary`；该对象只汇总固定 probe 结果 metadata，不替代既有 sibling 字段，不暴露完整 probe payload、error samples、endpoint response body 或 daemon control，不请求额外 probe、不改变健康分类、不证明 readiness、endpoint 覆盖、failure coverage 或 live provider availability，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.advisory_summary`，集中投影既有 probe status、request coverage、total/requested/healthy/failed/unhealthy counts、requested/healthy/failed/unhealthy/problem presence flags、primary problem probe、primary error-sample probe 与 `read_only_probe_summary` boundary marker，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-advisory-summary`；该对象只是读者入口和 dashboard-friendly rollup，不替代 `request_summary` / `health_summary` / `outcome_summary`、count map、target list、单项 probe payload 或错误样本，不请求额外 probe、不改变健康分类、不证明 readiness、endpoint 覆盖、failure coverage、broker readiness、workflow readiness、live provider availability 或生产 daemon control，也不新增 socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 在 `summary_view.status_summary` 内新增只读 `probe_status` / `probe_request_coverage_status` / `has_problem_probe` / `primary_problem_probe`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-status-summary-probe-advisory-fields`；这些字段只投影已构造的 `runtime.probe_summary.advisory_summary` 或 sibling summary metadata，让 summary view 第一屏可读，不替代 copied `probe_summary`、详细 `status`、单项 probe payload、count map 或错误样本，不执行额外 probe、不改变 probe opt-in 语义、不证明 readiness、endpoint 覆盖、broker readiness、workflow readiness、live provider availability 或生产 daemon control，也不新增 socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史设计登记（当时状态为 `[部分实现]`，当时 daemon lifecycle 子能力为 `[已设计/待实现]`）：provider-replay daemon lifecycle 的 `start` / `stop` / lifecycle `status` / explicit `restart` / supervised `backoff` contract 最初在 OpenSpec `provider-replay-daemon-lifecycle-design` 中登记，证据为 `openspec/changes/provider-replay-daemon-lifecycle-design/design.md` 与 `openspec/changes/provider-replay-daemon-lifecycle-design/specs/tdx-provider-transport-replay-service/spec.md`；该设计要求 start 必须显式 config + operator action 并记录 ownership metadata，stop/restart 必须证明 controller ownership，status 必须区分 configured capability、owned process、observed HTTP health 与 stale/missing ownership metadata，backoff 必须 opt-in、bounded 并暴露 retry/delay/last-failure/blocked 状态；这是早期设计来源记录，当前 E-06 可用边界以主行和收口登记为准，且仍不代表真实 provider lifecycle、broker readiness、workflow readiness、live provider availability 或生产 daemon control。

> E-06 历史补充登记（当时状态为 `[部分实现]`，当时 daemon lifecycle 控制为 `[已设计/待实现]`）：`provider-replay status --view summary` 与 detailed payload 新增只读 `lifecycle.ownership_summary` / `summary_view.lifecycle.ownership_summary`，证据为 `tdxquant/provider_transport_replay.py`、`tdxquant/cli.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-lifecycle-ownership-summary`；当前对象固定报告 `ownership_status=not_managed`、`owned_process=false`、`state_file_present=false`、`state_file_stale=false`、`control_allowed=false`、`status_source=configured_boundary` 与 `no_lifecycle_ownership; read_only_status` boundary，只是为未来 lifecycle controller 预留 ownership 状态槽，不读取/写入 pidfile 或 statefile、不扫描进程表、不从端口或 HTTP reachability 推断 ownership、不提供 start/stop/restart/backoff、supervisor loop、自动恢复、broker readiness、workflow readiness、live provider availability 或生产 daemon control。

> E-06 历史补充登记（当时状态为 `[部分实现]`，当时 daemon lifecycle 控制为 `[已设计/待实现]`）：`provider-replay status --view summary` 与 detailed payload 新增只读 `lifecycle.control_summary` / `summary_view.lifecycle.control_summary`，证据为 `tdxquant/provider_transport_replay.py`、`tdxquant/cli.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-lifecycle-control-summary`；当前对象固定报告 `control_status=unsupported`、`control_allowed=false`、`available_operations=[]`、`blocked_operations=[start, stop, restart, backoff]`、`blocking_reason=lifecycle_control_not_implemented`、`ownership_required=true`、`operator_action_required=true` 与 `read_only_lifecycle_status; no_control_operations` boundary，只是明确当前 lifecycle control 全部被阻断，不实现 start/stop/restart/backoff、scheduler、supervisor、pidfile/statefile、自动恢复、进程表扫描、端口 ownership 推断、broker readiness、workflow readiness、live provider availability 或生产 daemon control。

> E-06 历史补充登记（当时状态为 `[部分实现]`，当时 daemon lifecycle 控制为 `[已设计/待实现]`）：`provider-replay status --view summary` 与 detailed payload 新增只读 `lifecycle.operation_summary` / `summary_view.lifecycle.operation_summary`，证据为 `tdxquant/provider_transport_replay.py`、`tdxquant/cli.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-lifecycle-operation-summary`；当前对象逐项登记 `start` / `stop` / `restart` / `backoff` 均为 `status=blocked`、`implemented=false`、`blocking_reason=lifecycle_control_not_implemented`，并显式标出 ownership/operator action 要求，只是 per-operation 只读矩阵，不实现这些命令、不改变 `control_summary` 阻断结论、不读取/写入 pidfile 或 statefile、不扫描进程表、不从端口或 HTTP reachability 推断 ownership、不证明 broker readiness、workflow readiness、live provider availability 或生产 daemon control。

> E-06 历史补充登记（当时状态为 `[部分实现]`，当时 daemon lifecycle 控制为 `[已设计/待实现]`）：`provider-replay status --view summary` 与 detailed payload 新增只读 `lifecycle.backoff_summary` / `summary_view.lifecycle.backoff_summary`，证据为 `tdxquant/provider_transport_replay.py`、`tdxquant/cli.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-lifecycle-backoff-summary`；当前对象固定报告 `backoff_status=not_configured`、`enabled=false`、`policy=not_managed`、`retry_count=0`、`delay_window_seconds=null`、`last_failure_reason=null`、`next_retry_status=not_scheduled`、`next_retry_pending=false`、`blocked=true` 与 `blocking_reason=lifecycle_control_not_implemented`，只是 future supervised backoff 的只读状态槽，不实现 retry timer、scheduler、supervisor、自动恢复、restart/backoff policy、pidfile/statefile、进程表扫描、端口 ownership 推断、broker readiness、workflow readiness、live provider availability 或生产 daemon control。

> E-06 历史补充登记（当时状态为 `[部分实现]`，当时 daemon lifecycle 控制为 `[已设计/待实现]`）：`provider-replay status --view summary` 与 detailed payload 新增只读 `lifecycle.supervision_summary` / `summary_view.lifecycle.supervision_summary`，证据为 `tdxquant/provider_transport_replay.py`、`tdxquant/cli.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-lifecycle-supervision-summary`；当前对象固定报告 `supervision_status=not_supervised`、`supervisor_configured=false`、`supervisor_type=none`、`managed_process_count=0`、`active_process_count=0`、`desired_state=unmanaged`、`observed_state=not_observed`、`process_identity_status=not_tracked`、`state_file_status=not_configured`、`pid_status=not_tracked`、`control_allowed=false`、`blocked=true` 与 `blocking_reason=lifecycle_control_not_implemented`，只是 future supervisor/process tracking 的只读状态槽，不实现 start/stop/restart、daemonization、supervisor loop、pid/state tracking、进程 ownership、process table scan、port ownership 推断、retry/backoff timer、scheduler、自动恢复、broker readiness、workflow readiness、live provider availability 或生产 daemon control。

> E-06 历史补充登记（当时状态为 `[部分实现]`，当时 daemon lifecycle 控制为 `[已设计/待实现]`）：`provider-replay status --view summary` 的 `summary_view.status_summary` 新增只读 supervision rollup 字段 `lifecycle_supervision_status`、`lifecycle_supervisor_configured`、`lifecycle_desired_state`、`lifecycle_observed_state` 与 `lifecycle_process_identity_status`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-status-supervision-rollup`；这些字段只是从 `lifecycle.supervision_summary` 派生的一屏摘要，不读取/写入 pidfile 或 statefile，不扫描进程表，不从端口/HTTP 可达性推断 ownership，不实现 start/stop/restart、daemonization、supervisor loop、retry/backoff timer、scheduler、自动恢复、broker readiness、workflow readiness、live provider availability 或生产 daemon control。

> E-06 历史补充登记（当时状态为 `[部分实现]`，当时 daemon lifecycle 控制为 `[已设计/待实现]`）：provider replay config 新增可解析但不执行的 `lifecycle_state_file` 配置槽，`provider-replay status` detailed payload 新增只读 `lifecycle.statefile_summary`，`provider-replay config-check --view summary` 新增 `lifecycle_state_file_provided`、`statefile_inspected=false` 与 `statefile_written=false`，证据为 `tdxquant/provider_transport_replay.py`、`tdxquant/cli.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-lifecycle-statefile-boundary`；当前只报告 `not_configured` 或 `configured_not_inspected`，不读取/写入/创建/删除/加锁 statefile，不做 state schema、stale detection、pid ownership、process table scan、port ownership 推断、start/stop/restart、daemonization、supervisor loop、retry/backoff timer、scheduler、自动恢复、broker readiness、workflow readiness、live provider availability 或生产 daemon control。

> E-06 历史补充登记（当时状态为 `[部分实现]`，当时 daemon lifecycle 控制为 `[已设计/待实现]`）：CLI 新增只读 `provider-replay lifecycle-plan --operation start|stop|restart|backoff`，可返回 detailed `plan.execution_mode=non_executing_lifecycle_plan` 与 summary view，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-lifecycle-control-plan`；该 plan 只从现有 lifecycle status、operation_summary、statefile_summary 与 supervision_summary 派生 blocked/control-not-implemented 结果，`dispatch_executed=false`，不调用 `serve`、不 probe runtime、不读取/写入 statefile、不扫描进程表、不从端口/HTTP 可达性推断 ownership，不实现 start/stop/restart/backoff、daemonization、supervisor loop、retry/backoff timer、scheduler、自动恢复、broker readiness、workflow readiness、live provider availability 或生产 daemon control。

> E-06 历史补充登记（当时状态为 `[部分实现]`，当时 daemon lifecycle 控制为 `[已设计/待实现]`）：CLI 新增显式只读 `provider-replay lifecycle-state-check --config --stale-after-seconds --view detailed|summary`，可读取已配置 `lifecycle_state_file` 并输出 `statefile_check.check_status/schema_valid/provider_id_matches/stale/error_count/control_allowed=false`，证据为 `tdxquant/provider_transport_replay.py`、`tdxquant/cli.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-lifecycle-statefile-check`；该命令只在调用时读取 statefile JSON，校验 `tdx.provider_replay.lifecycle_state.v1` 最小 schema、provider_id、pid、state 与 updated_at/stale 状态，不写入/创建/删除/加锁 statefile，不扫描进程表，不从端口/HTTP 可达性推断 ownership，不调用 `serve` 或 runtime probe，不实现 start/stop/restart/backoff、daemonization、supervisor loop、retry/backoff timer、scheduler、自动恢复、broker readiness、workflow readiness、live provider availability 或生产 daemon control；即使检查为 valid，也不是进程 ownership、readiness 或执行授权证明。

> E-06 历史补充登记（当时状态为 `[部分实现]`，当时 daemon lifecycle 控制为 `[已设计/待实现]`）：`provider-replay lifecycle-plan` 新增 opt-in `--include-statefile-check --stale-after-seconds`，可在 non-executing plan 中嵌入紧凑 `statefile_diagnostics`、`statefile_check_status`、`statefile_schema_valid`、`statefile_provider_id_matches` 与 `statefile_stale`，summary view 同步投影，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-lifecycle-plan-statefile-diagnostics`；默认不读取 statefile，只有显式 include 时才复用 `lifecycle-state-check` 的只读诊断，且 `dispatch_executed=false`、`control_allowed=false` 不变，不写入/加锁 statefile，不调用 `serve` 或 runtime probe，不扫描进程表，不从端口/HTTP 可达性推断 ownership，不实现 start/stop/restart/backoff、daemonization、supervisor loop、retry/backoff timer、scheduler、自动恢复、broker readiness、workflow readiness、live provider availability 或生产 daemon control；valid diagnostics 只是计划上下文，不是进程 ownership、readiness 或执行授权证明。

> E-06 历史补充登记（当时状态为 `[部分实现]`，当时 daemon lifecycle 控制为 `[已设计/待实现]`）：`provider-replay status --view summary` 在 `summary_view.status_summary` 内新增只读 `lifecycle_operation_count` / `lifecycle_available_operation_count` / `lifecycle_blocked_operation_count` / `lifecycle_primary_blocked_operation`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-status-summary-lifecycle-operation-counts`；这些字段只投影已构造的 `summary_view.lifecycle.operation_summary`，让 summary view 第一屏直接显示 lifecycle operation 全部被阻断的计数和首个 blocked operation，不替代 detailed `status`、`summary_view.lifecycle.operation_summary` 或后续 lifecycle controller 设计，不实现 start/stop/restart/backoff、scheduler、supervisor、pidfile/statefile、自动恢复、进程表扫描、端口 ownership 推断、broker readiness、workflow readiness、live provider availability 或生产 daemon control。

> E-06 历史补充登记（当时状态为 `[部分实现]`，当时 daemon lifecycle 控制为 `[已设计/待实现]`）：`provider-replay status --view summary` 在 `summary_view.status_summary` 内新增只读 `lifecycle_ownership_status` / `lifecycle_owned_process` / `lifecycle_control_status` / `lifecycle_blocking_reason`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-status-summary-lifecycle-control-fields`；这些字段只投影已构造的 `summary_view.lifecycle.ownership_summary` / `control_summary`，让 summary view 第一屏直接显示 lifecycle ownership/control 被阻断的状态，不替代 detailed `status`、`summary_view.lifecycle`、ownership/control summary 对象或后续 lifecycle controller 设计，不实现 start/stop/restart/backoff、scheduler、supervisor、pidfile/statefile、自动恢复、进程表扫描、端口 ownership 推断、broker readiness、workflow readiness、live provider availability 或生产 daemon control。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.primary_failed_probe`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-primary-failed-probe`；该字段只从既有 `failed` 目标列表派生首个 failed probe，`null` 只表示当前没有 failed probe，不替代 `failed_count` / `failed_status_counts` / `error_samples`，不证明健康状态或 endpoint 覆盖，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.primary_unhealthy_probe`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-primary-unhealthy-probe`；该字段只从既有 `unhealthy` 目标列表派生首个 unhealthy probe，`null` 只表示当前没有 unhealthy probe，不替代 `unhealthy_count`、`failed`、`primary_failed_probe` 或单项 probe 证据，不证明服务健康、readiness、endpoint 覆盖或生产 daemon 控制，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.primary_error_sample_probe` / `primary_error_sample_status`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py` 与 OpenSpec `provider-replay-primary-error-sample`；这些字段只从既有有界 `error_samples` 列表首个样本派生 probe/status 身份，`null` 只表示当前没有 error sample，不替代 `error_sample_count`、`error_sample_status_counts`、`error_sample_probe_counts`、单项 probe 证据或完整错误 payload，不证明服务健康、readiness、endpoint 覆盖或生产 daemon 控制，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.primary_error_sample_error_code` / `primary_error_sample_http_status`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py` 与 OpenSpec `provider-replay-primary-error-sample-diagnostics`；这些字段只从既有有界 `error_samples` 列表首个样本派生 error_code/http_status 紧凑诊断，`null` 只表示首个样本无对应紧凑字段，不替代 `error_code_counts`、`failed_error_code_counts`、`failed_http_status_counts`、单项 probe 证据或完整错误 payload，不证明服务健康、readiness、endpoint 覆盖或生产 daemon 控制，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.primary_error_sample_reachability`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-primary-error-sample-reachability`；该字段只从首个错误样本候选派生 `reachable` / `unreachable` / `unknown` 紧凑诊断，`null` 只表示当前没有错误样本候选，不改变 `error_samples` payload 形状或排序，不替代 `error_sample_reachability_counts`、`requested_reachability_counts`、`failed_reachability_counts` 或单项 probe 证据，不证明服务健康、readiness、endpoint 覆盖或生产 daemon 控制，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.error_sample_hidden_count`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py` 与 OpenSpec `provider-replay-error-sample-hidden-count`；该字段只从既有 `error_sample_count` 与有界 `error_samples` 长度派生被 sample limit 隐藏的候选数量，不改变 `error_sample_limit`、`error_sample_truncated` 或 sample 排序，不暴露完整错误 payload，不证明服务健康、readiness、endpoint 覆盖或生产 daemon 控制，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.error_sample_visible_count`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py` 与 OpenSpec `provider-replay-error-sample-visible-count`；该字段只从既有有界 `error_samples` 列表长度派生当前响应可见样本数量，不替代 `error_sample_count`、`error_sample_hidden_count`、`error_sample_limit` 或 `error_sample_truncated`，不改变 sample limit 或 sample 排序，不暴露完整错误 payload，不证明服务健康、readiness、endpoint 覆盖或生产 daemon 控制，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.error_sample_summary`，集中投影既有 `error_sample_count` / `error_sample_visible_count` / `error_sample_hidden_count` / `error_sample_limit` / `error_sample_truncated` 与 `primary_error_sample_probe` / `primary_error_sample_status` / `primary_error_sample_error_code` / `primary_error_sample_http_status` / `primary_error_sample_reachability`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-error-sample-summary`；该对象只汇总错误样本的 compact metadata，不替代既有 sibling 字段，不暴露完整 probe payload 或完整错误样本列表，不证明服务健康、readiness、endpoint 覆盖或生产 daemon 控制，也不新增 probe 端点、socket 启动、provider mutation、调度、retry、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.primary_healthy_probe`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-primary-healthy-probe`；该字段只从既有 `healthy` 目标列表派生首个 healthy probe，`null` 只表示当前没有 healthy probe，不替代 `healthy_count` / `healthy_reachability_counts` / 单项 probe 证据，不证明服务健康、readiness 或 endpoint 覆盖，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.primary_not_requested_probe`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-primary-not-requested-probe`；该字段只从既有 `not_requested` 目标列表派生首个未请求 probe，`null` 只表示当前没有 not-requested probe，不替代 `not_requested_count` / `request_coverage_status` / 单项 probe 证据，不证明该目标已被 probe、失败、不可用或需要执行，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.failed_reachability_counts`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-failed-reachability-counts`；该字段只统计已请求且非 `healthy` probe 的 `reachable` 分桶并排除 `healthy`/`not_requested`，不替代 `requested_reachability_counts` / `failed_status_counts` / 单项 probe 证据，不证明健康状态、失败覆盖或 endpoint 覆盖，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.healthy_reachability_counts`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-healthy-reachability-counts`；该字段只统计已请求且 `status=healthy` probe 的 `reachable` 分桶并排除 failed/`not_requested`，不替代 `requested_reachability_counts` / `healthy_http_status_counts` / 单项 probe 证据，不证明服务健康、HTTP 覆盖或 endpoint 覆盖，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读顶层 `runtime.probe_summary.primary_problem_probe`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-primary-problem-probe`；该字段只从既有 normalized probe summary 派生首个需关注 probe（优先 failed，其次 unhealthy，再次 error sample），并与 `outcome_summary.primary_problem_probe` 保持一致，`null` 只表示当前没有 failed/unhealthy/error-sample probe，不替代单项 probe payload、count map、`health_summary` 或 `outcome_summary`，不证明服务健康、readiness、endpoint 覆盖或生产 daemon 控制，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.has_error_sample`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-has-error-sample`；该字段只从既有 `error_sample_count` 派生是否存在 probe error sample，不替代 `error_samples`、`error_sample_summary`、primary error-sample 字段、count map 或单项 probe payload，不改变 sample limit、sample 排序、probe 执行、健康分类或 degraded 判定，不证明服务健康、readiness、endpoint 覆盖或生产 daemon 控制，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.has_hidden_error_sample`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-has-hidden-error-sample`；该字段只从既有 `error_sample_hidden_count` 派生是否有 probe error sample 被 sample limit 隐藏，不替代 `error_sample_hidden_count`、`error_sample_truncated`、`error_sample_summary`、`error_samples` 或单项 probe payload，不改变 sample limit、sample 排序、probe 执行、健康分类或 degraded 判定，不证明服务健康、readiness、endpoint 覆盖或生产 daemon 控制，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读 `runtime.probe_summary.has_visible_error_sample`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-has-visible-error-sample`；该字段只从既有 `error_sample_visible_count` 派生当前 bounded response 是否含可见 probe error sample，不替代 `error_sample_visible_count`、`error_samples`、`error_sample_summary`、`has_error_sample` 或单项 probe payload，不改变 sample limit、sample 排序、probe 执行、健康分类或 degraded 判定，不证明服务健康、readiness、endpoint 覆盖或生产 daemon 控制，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读顶层 `runtime.probe_summary.has_healthy_probe`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-has-healthy-probe`；该字段只从既有 normalized `healthy` / `healthy_count` 派生是否存在健康 probe，不替代 `healthy_count`、`healthy`、`primary_healthy_probe`、`health_summary`、单项 probe payload 或 count map，不改变 probe 执行、健康分类、degraded 判定、sample limit 或 summary 子结构，不证明服务健康、readiness、endpoint 覆盖、broker readiness、生产 daemon 控制或写能力，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读顶层 `runtime.probe_summary.has_not_requested_probe`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-has-not-requested-probe`；该字段只从既有 normalized `not_requested` / `not_requested_count` 派生是否存在未请求 probe，不替代 `not_requested_count`、`not_requested`、`primary_not_requested_probe`、`request_summary`、`outcome_summary`、单项 probe payload 或 count map，不改变 request coverage 计算、probe 执行、健康分类、degraded 判定、sample limit 或 summary 子结构，不证明 provider 失败、服务健康、readiness、endpoint 覆盖、broker readiness、生产 daemon 控制或写能力，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读顶层 `runtime.probe_summary.all_probes_requested`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-all-requested-flag`；该字段只复用既有 request coverage 计算表达当前 status 请求是否覆盖全部已配置 probe，并与 `outcome_summary.all_probes_requested` 保持一致，不替代 `request_coverage_status`、`requested_count`、`not_requested_count`、`has_not_requested_probe`、`request_summary`、`outcome_summary` 或单项 probe payload，不改变 request coverage 计算、probe 执行、健康分类、degraded 判定、sample limit 或 summary 子结构，不证明服务健康、readiness、endpoint 语义覆盖、broker readiness、生产 daemon 控制或写能力，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读顶层 `runtime.probe_summary.has_requested_probe`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-has-requested-probe`；该字段只从既有 normalized `requested` / `requested_count` 派生当前 status 请求是否请求过至少一个 probe，不替代 `requested_count`、`requested`、`primary_requested_probe`、`request_summary`、`outcome_summary`、`all_probes_requested` 或单项 probe payload，不改变 request coverage 计算、probe 执行、健康分类、degraded 判定、sample limit 或 summary 子结构，不证明服务健康、readiness、endpoint 语义覆盖、broker readiness、生产 daemon 控制或写能力，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读顶层 `runtime.probe_summary.has_failed_probe`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-top-level-has-failed-probe`；该字段只从既有 normalized `failed` / `failed_count` 派生是否存在失败的已请求 probe，并与 `outcome_summary.has_failed_probe` 保持一致，不替代 `failed_count`、`failed`、`primary_failed_probe`、`failed_*_counts`、`outcome_summary` 或单项 probe payload，不改变 request coverage 计算、probe 执行、健康分类、degraded 判定、sample limit 或 summary 子结构，不证明服务健康、readiness、endpoint 语义覆盖、broker readiness、生产 daemon 控制或写能力，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读顶层 `runtime.probe_summary.has_unhealthy_probe`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-top-level-has-unhealthy-probe`；该字段只从既有 normalized `unhealthy` / `unhealthy_count` 派生是否存在 unhealthy probe，并与 `outcome_summary.has_unhealthy_probe` 保持一致，不替代 `unhealthy_count`、`unhealthy`、`primary_unhealthy_probe`、`status_counts`、`outcome_summary` 或单项 probe payload，不改变 request coverage 计算、probe 执行、健康分类、degraded 判定、sample limit 或 summary 子结构，不证明服务健康、readiness、endpoint 语义覆盖、broker readiness、生产 daemon 控制或写能力，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --probe-* --view summary` 与 detailed payload 新增只读顶层 `runtime.probe_summary.has_problem_probe`，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-probe-has-problem-probe`；该字段只从既有 normalized `primary_problem_probe` 派生是否已选出问题 probe，不替代 `primary_problem_probe`、`has_failed_probe`、`has_unhealthy_probe`、`has_error_sample`、`error_sample_summary`、`outcome_summary` 或单项 probe payload，不改变 problem probe 选择顺序、request coverage 计算、probe 执行、健康分类、degraded 判定、sample limit 或 summary 子结构，不证明服务健康、readiness、endpoint 语义覆盖、broker readiness、生产 daemon 控制或写能力，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label followup` 和 `--view summary` 新增只读 `bundle_step_source_entry_counts`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-bundle-step-source-entry-counts`；该字段只统计已解析 selected bundle step 的 `source:entry` 分布，不列出完整 bundle/step manifest，不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、执行覆盖或 readiness 证明。
>
> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label followup` 和 `--view summary` 新增只读 `task_report_bundle_step_source_entry_counts`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-task-report-step-source-entry-counts`；该字段只统计已解析 task+report bundle step 的 `source:entry` 分布，不列出完整 step manifest，不执行 task/report/trade/catalog entry/bundle step，也不代表 workflow builder、执行覆盖或 readiness 证明。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label followup` 和 `--view summary` 新增只读 `bundle_step_source_option_key_counts`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-bundle-step-source-option-key-counts`；该字段只统计已解析 selected bundle step 的 `source:option_key` 分布，不暴露 option value、不校验 option 语义、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、执行覆盖或 readiness 证明。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label followup` 和 `--view summary` 新增只读 `task_report_bundle_step_source_option_key_counts`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-task-report-step-source-option-key-counts`；该字段只统计已解析 task+report bundle step 的 `source:option_key` 分布，不暴露 option value、不校验 option 语义、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、执行覆盖或 readiness 证明。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label followup` 和 `--view summary` 新增只读 `submit_once_bundle_step_option_key_counts` 与 `pingan_bundle_step_option_key_counts`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-submit-pingan-step-option-key-counts`；这两个字段只统计已解析 submit-once/PingAn bundle 子集的 option key 分布，空 map 只表示当前选集无 option key，不暴露 option value、不校验 option 语义、不执行 catalog entry/task/report/trade/bundle step，也不代表 broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label followup` 和 `--view summary` 新增只读 `submit_once_bundle_step_source_option_key_counts` 与 `pingan_bundle_step_source_option_key_counts`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-submit-pingan-step-source-option-key-counts`；这两个字段只统计已解析 submit-once/PingAn bundle 子集的 `source:option_key` 分布，空 map 只表示当前选集无 source-qualified option key，不暴露 option value、不校验 option 语义、不执行 catalog entry/task/report/trade/bundle step，也不代表 broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label followup` 和 `--view summary` 新增只读 `submit_once_bundle_step_count` 与 `pingan_bundle_step_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-submit-pingan-step-counts`；这两个字段只统计已解析 submit-once/PingAn bundle 子集的 resolved step 总数，不列出完整 step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label followup` 和 `--view summary` 新增只读 `submit_once_bundle_step_source_entry_counts` 与 `pingan_bundle_step_source_entry_counts`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-submit-pingan-step-source-entry-counts`；这两个字段只统计已解析 submit-once/PingAn bundle 子集的 `source:entry` 分布，不列出完整 step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label followup|submit-once|pingan --view summary` 新增只读 `task_report_bundle_sample_count` / `submit_once_bundle_sample_count` / `pingan_bundle_sample_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-bundle-sample-counts`；这些字段只从已投影的有界 bundle sample 数组派生可见样本数量，不替代完整 bundle count/step count、limit 或 truncated 标志，不列出完整 bundle/step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog list --kind entry|bundle --view summary` 新增只读 `available_entry_label_count` / `available_bundle_label_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-available-label-counts`；这两个字段只从 summary 已投影的 `available_entry_labels` / `available_bundle_labels` 派生 label discovery 数量，不替代 `matched_entry_count` / `matched_bundle_count`，不列出完整 catalog manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog plan|preview --bundle ... --view summary` 新增只读 `step_source_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-plan-step-source-key-count`；该字段只从已投影的 selected bundle `step_source_counts` map 派生不同 step source key 数量，不统计 resolved step 总数、不列出完整 bundle/step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、provider readiness、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog plan|preview --bundle ... --view summary` 新增只读 `step_name_counts` 与 `step_name_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-plan-step-name-counts`；这些字段只从 selected bundle steps 的 `name` 字段派生 step name 分布和不同 name key 数量，不替代 `selected_step_count`、不列出完整 bundle/step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、provider readiness、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog plan|preview --bundle ... --view summary` 新增只读 `step_entry_counts` 与 `step_entry_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-plan-step-entry-counts`；这些字段只从 selected bundle steps 的 `entry` 字段派生 step entry 分布和不同 entry key 数量，不替代 `selected_step_count`、不暴露 option value 或完整 resolved args、不列出完整 bundle/step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、provider readiness、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog plan|preview --bundle ... --view summary` 新增只读 `step_source_name_counts` 与 `step_source_name_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-plan-step-source-name-counts`；这些字段只从 selected bundle steps 的 `source:name` 组合派生 source-qualified step name 分布和不同 key 数量，不替代 `selected_step_count`、不暴露 option value 或完整 resolved args、不列出完整 bundle/step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、provider readiness、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog plan|preview --bundle ... --view summary` 新增只读 `step_source_entry_counts` 与 `step_source_entry_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-plan-step-source-entry-counts`；这些字段只从 selected bundle steps 的 `source:entry` 组合派生 source-qualified step entry 分布和不同 key 数量，不替代 `selected_step_count`、不暴露 option value 或完整 resolved args、不列出完整 bundle/step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、provider readiness、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog plan|preview --bundle ... --view summary` 新增只读 `step_resolved_arg_key_counts` / `step_resolved_arg_key_count` / `step_source_resolved_arg_key_counts` / `step_source_resolved_arg_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-plan-step-resolved-arg-key-counts`；这些字段只从 selected bundle steps 的 reduced `resolved_args` key 与 `source:key` 组合派生分布和不同 key 数量，不暴露 resolved arg value、raw option value 或完整 resolved args，不列出完整 bundle/step manifest，不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、provider readiness、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label followup --view summary` 新增只读 `bundle_step_source_key_count` 与 `task_report_bundle_step_source_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-step-source-key-counts`；这两个字段只从已投影的 `bundle_step_source_counts` / `task_report_bundle_step_source_counts` map 派生不同 source key 数量，不统计 resolved step 总数、不列出完整 bundle/step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label followup --view summary` 新增只读 `bundle_step_name_key_count` / `bundle_step_entry_key_count` / `task_report_bundle_step_name_key_count` / `task_report_bundle_step_entry_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-step-name-entry-key-counts`；这些字段只从已投影的 selected bundle 与 task+report step name/entry count map 派生不同 key 数量，不统计 resolved step 总数、不列出完整 bundle/step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label followup --view summary` 新增只读 `bundle_step_source_name_key_count` / `bundle_step_source_entry_key_count` / `task_report_bundle_step_source_name_key_count` / `task_report_bundle_step_source_entry_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-step-source-name-entry-key-counts`；这些字段只从已投影的 selected bundle 与 task+report step `source:name` / `source:entry` count map 派生不同 key 数量，不统计 resolved step 总数、不列出完整 bundle/step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label followup --view summary` 新增只读 `bundle_step_option_key_count` / `bundle_step_source_option_key_count` / `task_report_bundle_step_option_key_count` / `task_report_bundle_step_source_option_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-bundle-task-report-option-key-counts`；这些字段只从已投影的 selected bundle 与 task+report step option-key / `source:option_key` count map 派生不同 key 数量，不暴露 option value、不校验 option 语义、不统计 resolved step 总数、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label followup --view summary` 新增只读 `bundle_label_key_count` 与 `task_report_bundle_label_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-bundle-label-key-counts`；这两个字段只从已投影的 `bundle_label_counts` / `task_report_bundle_label_counts` map 派生不同 label key 数量，不统计 bundle 总数、不列出完整 bundle manifest 或完整 label assignment、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label followup|diagnostics --view summary` 新增只读 `task_report_bundle_summary`，集中投影既有 `task_report_bundle_count` / `task_report_bundle_step_count` / `task_report_bundle_sample_count` / `task_report_bundle_sample_limit` / `task_report_bundle_sample_truncated` 与 label/source/name/entry/source-entry/option-key/source-option-key map 的 key counts，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-task-report-bundle-summary`；该对象只汇总 task/report bundle validation summary metadata，不替代既有 sibling 字段，不暴露 option values、resolved args、完整 bundle manifest 或完整 step manifest，不执行 catalog entry、task、report、trade 或 bundle step，也不代表 workflow builder、broker readiness、交易安全证明、执行覆盖或可用性证明。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle|entry --label followup|report --view summary` 新增只读 `bundle_step_summary`，集中投影既有 `bundle_count` / `bundle_step_count` 与 label/source/name/entry/source-name/source-entry/option-key/source-option-key map 的 key counts，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-bundle-step-summary`；该对象只汇总 selected bundle validation summary metadata，`--kind entry` 或无 selected bundle 时可为 `0`，不替代既有 sibling 字段，不暴露 option values、resolved args、完整 bundle manifest 或完整 step manifest，不执行 catalog entry、task、report、trade 或 bundle step，也不代表 workflow builder、broker readiness、交易安全证明、执行覆盖或可用性证明。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind entry|bundle --label report|followup --view summary` 新增只读 `entry_summary`，集中投影既有 `entry_count` / `entry_source_key_count` / `entry_label_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-entry-summary`；该对象只汇总 selected entry validation summary metadata，`--kind bundle` 或无 selected entry 时可为 `0`，不替代既有 sibling 字段，不暴露 option values、resolved args、完整 entry manifest、完整 bundle manifest 或完整 step manifest，不执行 catalog entry、task、report、trade 或 bundle step，也不代表 workflow builder、broker readiness、交易安全证明、执行覆盖或可用性证明。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label submit-once|pingan --view summary` 新增只读 `submit_once_bundle_summary` 与 `pingan_bundle_summary`，集中投影既有 submit-once/PingAn bundle count、step count、sample metadata 及 label/source/name/entry/source-entry/option-key/source-option-key map 的 key counts，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-submit-pingan-bundle-summaries`；这两个对象只汇总 submit-once/PingAn bundle validation summary metadata，不替代既有 sibling 字段，不暴露 option values、resolved args、完整 bundle manifest 或完整 step manifest，不执行 catalog entry、task、report、trade 或 bundle step，也不代表 workflow builder、broker readiness、交易安全证明、执行覆盖或可用性证明。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label followup --view summary` 新增只读 `bundle_samples` / `bundle_sample_count` / `bundle_sample_limit` / `bundle_sample_truncated`，并在 `bundle_step_summary` 中镜像 `sample_count` / `sample_limit` / `sample_truncated`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-bundle-step-sample-summary`；这些字段只从 selected resolved bundle 名称派生 bounded sample metadata，不暴露完整 bundle manifest、完整 step manifest、option values、resolved args 或 executable instructions，不执行 catalog entry、task、report、trade、provider call 或 bundle step，也不代表 workflow builder、broker readiness、交易安全证明、执行覆盖或可用性证明。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label ... --view summary` 新增只读 `bundle_summary`，集中投影 selected bundle/label、bundle count、bundle step count、bounded bundle sample metadata、bundle label key count 与 bundle presence flags，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-bundle-summary`；该对象只从已投影的 bundle validation summary sibling 字段派生 bundle rollup，不替代 `bundle_step_summary` 或既有 sibling 字段，不暴露完整 entry manifest、完整 bundle manifest、完整 step manifest、option values、resolved args 或 executable instructions，不执行 catalog entry、task、report、trade、provider call 或 bundle step，也不代表 workflow builder、broker readiness、交易安全证明、执行覆盖或可用性证明。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind entry|bundle --label ... --view summary` 新增只读 `catalog_summary`，集中投影 validate mode/kind/selected filters、valid/invalid/non-execution 状态、entry/bundle/bundle-step counts、label/source key totals 与 high-level presence flags，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-catalog-summary`；该对象只从已投影的 `validation_outcome`、`entry_summary`、`bundle_summary`、`label_summary` 与 `source_summary` sibling metadata 派生顶层 registry rollup，不替代既有 summary 对象或 sibling 字段，不暴露完整 entry manifest、完整 bundle manifest、完整 step manifest、option values、resolved args 或 executable instructions，不执行 catalog entry、task、report、trade、provider call 或 bundle step，也不代表 workflow builder、broker readiness、交易安全证明、执行覆盖或可用性证明。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog plan|preview --bundle ... --view summary` 新增只读 `plan_summary`，集中投影 target identity、execution mode、non-execution/dispatch 状态、selected step range/count、step source/name/entry/resolved-arg key counts 与 step presence flags，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-plan-summary`；该对象只从已投影的 `plan_outcome`、`selected_step_summary` 与 selected-step sibling metadata 派生 planning registry rollup，不替代既有 summary 对象或 sibling 字段，不暴露完整 entry manifest、完整 bundle manifest、raw resolved args、option values 或 executable instructions，不执行 catalog entry、task、report、trade、provider call 或 bundle step，也不代表 workflow builder、broker readiness、交易安全证明、执行覆盖或可用性证明。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind entry|bundle --label ... --view summary` 新增只读 `label_summary`，集中投影 `selected_label`、entry/bundle label key count、selected label 的 entry/bundle 命中数及 distinct label key 总数，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-label-summary`；该对象只从已投影的 `entry_label_counts` / `bundle_label_counts` map 派生 label rollup，不替代既有 sibling 字段，不暴露完整 entry manifest、完整 bundle manifest、完整 step manifest、option values、resolved args 或 executable instructions，不执行 catalog entry、task、report、trade、provider call 或 bundle step，也不代表 workflow builder、broker readiness、交易安全证明、执行覆盖或可用性证明。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind entry|bundle --label ... --view summary` 新增只读 `source_summary`，集中投影 entry source key count、bundle-step source key count、entry count、bundle step count 及 distinct source key 总数，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-source-summary`；该对象只从已投影的 `entry_source_counts` / `bundle_step_source_counts` map 派生 source rollup，不替代既有 sibling 字段，不暴露完整 entry manifest、完整 bundle manifest、完整 step manifest、option values、resolved args 或 executable instructions，不执行 catalog entry、task、report、trade、provider call 或 bundle step，也不代表 workflow builder、broker readiness、交易安全证明、执行覆盖或可用性证明。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --view summary` 新增只读 `entry_source_key_count` 与 `entry_label_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-entry-source-label-key-counts`；这两个字段只从已投影的 `entry_source_counts` / `entry_label_counts` map 派生不同 source/label key 数量，不统计 matched entry 总数、不列出完整 entry manifest 或完整 label assignment、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label submit-once|pingan --view summary` 新增只读 `submit_once_bundle_label_key_count` 与 `pingan_bundle_label_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-submit-pingan-label-key-counts`；这两个字段只从已投影的 `submit_once_bundle_label_counts` / `pingan_bundle_label_counts` map 派生不同 label key 数量，不统计 bundle 总数、不列出完整 bundle manifest 或完整 label assignment、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label submit-once|pingan --view summary` 新增只读 `submit_once_bundle_step_source_key_count` 与 `pingan_bundle_step_source_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-submit-pingan-step-source-key-counts`；这两个字段只从已投影的 `submit_once_bundle_step_source_counts` / `pingan_bundle_step_source_counts` map 派生不同 source key 数量，不统计 resolved step 总数、不列出完整 bundle/step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label submit-once|pingan --view summary` 新增只读 `submit_once_bundle_step_name_key_count` 与 `pingan_bundle_step_name_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-submit-pingan-step-name-key-counts`；这两个字段只从已投影的 `submit_once_bundle_step_name_counts` / `pingan_bundle_step_name_counts` map 派生不同 step-name key 数量，不统计 resolved step 总数、不列出完整 bundle/step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label submit-once|pingan --view summary` 新增只读 `submit_once_bundle_step_source_name_key_count` 与 `pingan_bundle_step_source_name_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-submit-pingan-step-source-name-key-counts`；这两个字段只从已投影的 `submit_once_bundle_step_source_name_counts` / `pingan_bundle_step_source_name_counts` map 派生不同 `source:name` key 数量，不统计 resolved step 总数、不列出完整 bundle/step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label submit-once|pingan --view summary` 新增只读 `submit_once_bundle_step_entry_key_count` 与 `pingan_bundle_step_entry_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-submit-pingan-step-entry-key-counts`；这两个字段只从已投影的 `submit_once_bundle_step_entry_counts` / `pingan_bundle_step_entry_counts` map 派生不同 step-entry key 数量，不统计 resolved step 总数、不列出完整 bundle/step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label submit-once|pingan --view summary` 新增只读 `submit_once_bundle_step_source_entry_key_count` 与 `pingan_bundle_step_source_entry_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-submit-pingan-step-source-entry-key-counts`；这两个字段只从已投影的 `submit_once_bundle_step_source_entry_counts` / `pingan_bundle_step_source_entry_counts` map 派生不同 `source:entry` key 数量，不统计 resolved step 总数、不列出完整 bundle/step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label submit-once|pingan --view summary` 新增只读 `submit_once_bundle_step_option_key_count` 与 `pingan_bundle_step_option_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-submit-pingan-step-option-key-counts`；这两个字段只从已投影的 `submit_once_bundle_step_option_key_counts` / `pingan_bundle_step_option_key_counts` map 派生不同 option-key 数量，不暴露 option value、不校验 option 语义、不统计 resolved step 总数、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label submit-once|pingan --view summary` 新增只读 `submit_once_bundle_step_source_option_key_count` 与 `pingan_bundle_step_source_option_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-submit-pingan-step-source-option-key-counts`；这两个字段只从已投影的 `submit_once_bundle_step_source_option_key_counts` / `pingan_bundle_step_source_option_key_counts` map 派生不同 `source:option_key` 数量，不暴露 option value、不校验 option 语义、不统计 resolved step 总数、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --view summary` 新增只读 `status_summary`，集中投影 provider/transport/source、read-only/write support、endpoint/probe/control/boundary/runtime status counts，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-status-summary-rollup`；该对象只从已构建的 fake provider replay status payload 派生，不启动服务、不发起额外 probe、不管理 start/stop/restart/daemon lifecycle、不代表 fake provider 可用于生产或真实行情服务。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog plan --bundle ... --view summary` 的只读 `plan_summary` 新增 first/last selected step index/name/source/command hints，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-plan-summary-selected-step-hints`；这些字段只从既有 `selected_step_summary` 派生并保持一致，不列出完整 bundle/step manifest、不暴露 option value 或 resolved arg、不执行 catalog entry、bundle、task/report step、trade command、provider call 或 workflow action，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog plan --bundle ... --view summary` 的只读 `plan_summary` 新增 `first_step_entry` 与 `last_step_entry`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-plan-summary-selected-entry-hints`；这两个字段只从既有 `selected_step_summary` entry hints 派生并保持一致，不列出完整 bundle/step manifest、不暴露 option value 或 resolved arg、不执行 catalog entry、bundle、task/report step、trade command、provider call 或 workflow action，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog plan --bundle ... --view summary` 的只读 `selected_step_summary` 新增 `step_source_name_key_count` 与 `step_source_entry_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-plan-selected-step-pair-key-summary`；这两个字段只从既有 selected bundle plan 的 source/name 与 source/entry pair-key count 派生，不列出完整 bundle/step manifest、不暴露 option value 或 resolved arg、不执行 catalog entry、bundle、task/report step、trade command、provider call 或 workflow action，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog plan --bundle ... --view summary` 的只读 `plan_summary` 新增 `step_source_name_key_count` 与 `step_source_entry_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-plan-summary-source-pair-keys`；这两个字段只从既有 `selected_step_summary` source/name 与 source/entry pair-key count 派生，不列出完整 bundle/step manifest、不暴露 option value 或 resolved arg、不执行 catalog entry、bundle、task/report step、trade command、provider call 或 workflow action，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog validate --kind bundle --label followup --view summary` 新增只读 `validation_outcome`，集中投影既有 kind、selected label、entry/bundle count、invalid count、valid/non-execution、result code/message 及 conservative outcome booleans，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-validate-outcome-summary`；该对象只汇总 catalog registry validation metadata，不替代既有 count map、family summary 或 raw validation payload，不列出完整 entry/bundle manifest，不执行 catalog entry、bundle、task/report step、trade command、provider call 或 workflow action，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog plan --bundle confirm-complete-review --view summary` 新增只读 `plan_outcome`，集中投影既有 mode、target、selected step count、step-source key count、result code/message 与 non-execution constraints，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-plan-outcome-summary`；该对象只汇总 catalog plan metadata，不替代既有 `constraints`、`steps`、count map、trade boundary 或 summary 字段，不列出完整 entry/bundle manifest，不执行 catalog entry、bundle、task/report step、trade command、provider call 或 workflow action，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog plan --bundle confirm-complete-review --view summary` 新增只读 `selected_step_summary`，集中投影既有 selected from/to step、selected step count、first/last selected step hints 与 step source/name/entry key counts，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-plan-selected-step-summary`；该对象只汇总 catalog bundle plan 的 selected-step metadata，不替代既有 `steps`、count map、`constraints`、`plan_outcome` 或 raw bundle manifest，不执行 catalog entry、bundle、task/report step、trade command、provider call 或 workflow action，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog plan --bundle ... --view summary` 的只读 `selected_step_summary` 新增 `step_resolved_arg_key_count` 与 `step_source_resolved_arg_key_count`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-plan-selected-step-arg-key-summary`；这两个字段只从既有 selected-step 顶层 resolved-arg key count metadata 派生并保持一致，不暴露 resolved arg value、不列出完整 step/bundle manifest、不执行 catalog entry、bundle、task/report step、trade command、provider call 或 workflow action，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog plan --bundle ... --view summary` 的只读 `selected_step_summary` 新增 `first_step_source` 与 `last_step_source`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-plan-selected-step-source-hints`；这两个字段只从既有 selected step dispatch source metadata 派生 first/last source hint，不暴露完整 dispatch manifest、resolved arg value、完整 step/bundle manifest 或执行结果，不执行 catalog entry、bundle、task/report step、trade command、provider call 或 workflow action，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog plan --bundle ... --view summary` 的只读 `selected_step_summary` 新增 `first_step_command_name` 与 `last_step_command_name`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-plan-selected-step-command-hints`；这两个字段只从既有 selected step dispatch command-name metadata 派生 first/last command hint，不暴露完整 dispatch manifest、resolved arg value、完整 step/bundle manifest 或执行结果，不执行 catalog entry、bundle、task/report step、trade command、provider call 或 workflow action，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-11 补充登记（状态仍为 `[部分实现]`）：`catalog plan --bundle ... --view summary` 的只读 `selected_step_summary` 新增 `first_step_index` 与 `last_step_index`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `catalog-plan-selected-step-index-hints`；这两个字段只从既有 selected plan step index metadata 派生 first/last index hint，用于只读定位 selected slice，不暴露完整 step manifest、完整 dispatch manifest、resolved arg value、完整 bundle manifest 或执行结果，不执行 catalog entry、bundle、task/report step、trade command、provider call 或 workflow action，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay lifecycle-readiness --config --include-statefile-check --stale-after-seconds --view detailed/summary` 新增只读 lifecycle readiness 摘要，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-lifecycle-readiness-summary`；该命令默认不读取 statefile，显式 opt-in 时只把有效、未过期且 provider 匹配的 statefile 诊断计为 `valid_lifecycle_statefile` 单个先决条件，输出固定 `ready=false`、`control_allowed=false`、`dispatch_executed=false` 的 blocked 状态与缺口清单；不启动、停止、重启、daemonize、supervise、probe runtime、扫描进程、推断端口 ownership、写入/加锁 statefile、调度 retry，也不证明 broker/workflow/write readiness 或 daemon 生命周期控制已完成。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：新增内部 `write_provider_replay_lifecycle_statefile()`、`build_provider_replay_lifecycle_state_payload()` 与 `build_provider_replay_lifecycle_config_hash()`，可在 `lifecycle_state_file` 配置存在时用 `<statefile>.lock` 独占锁和 `os.replace()` 原子写入 schema/provider/pid/state/owner_token/generation/config_hash/updated_at ownership payload，并让 `check_provider_replay_lifecycle_statefile()` 只读回显 owner token、generation、config hash 与 match 诊断，证据为 `tdxquant/provider_transport_replay.py`、`tests/test_provider_transport_replay.py` 与 OpenSpec `provider-replay-daemon-statefile-ownership-lock`；该层只是 daemon lifecycle 的 statefile/lock/ownership 基础，不暴露 start/stop/restart CLI，不启动、停止、重启、daemonize、supervise、probe runtime、扫描进程、推断端口 ownership、调度 retry，也不证明 PID liveness、supervisor health、broker/workflow/write readiness 或真实 provider 生命周期控制。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay daemon start|status|stop --config ...` 新增基于 statefile ownership 的 one-shot managed replay daemon 控制面，底层 `start_provider_replay_managed_daemon()` 可后台启动 `provider-replay serve --config <path>` 并写入 running statefile，`get_provider_replay_managed_daemon_status()` 只读检查 statefile 与 PID liveness，`stop_provider_replay_managed_daemon()` 要求 owner token 匹配后只对 statefile 记录的 owned PID 发送 terminate 并写入 stopping statefile，证据为 `tdxquant/provider_transport_replay.py`、`tdxquant/cli.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-managed-daemon-control`；该能力只覆盖 replay daemon 的显式 start/status/stop，不包含长期 supervisor loop、自动 restart/backoff、端口 ownership 推断、进程命令行校验、真实 provider 管理、broker readiness、workflow readiness 或 write readiness。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay daemon supervise --config ...` 新增显式前台 supervisor loop，底层 `run_provider_replay_managed_daemon_supervisor()` 启动一个 replay child、写入 `state=supervising` heartbeat statefile、轮询 child exit，并在 child exit 时写入 `state=exited`、interrupt 时尝试 terminate child 并写入 `state=stopping`，证据为 `tdxquant/provider_transport_replay.py`、`tdxquant/cli.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-daemon-supervisor-loop`；该能力只覆盖单 child 的前台观察和 heartbeat 刷新，不自动重启、不调度 backoff、不做端口 ownership 推断、不验证进程命令行、不管理真实 provider，也不证明 broker/workflow/write readiness。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay daemon supervise --restart-policy never|on-failure --max-restarts --backoff-seconds` 新增显式 opt-in 的 supervisor restart/backoff 策略，默认仍为 `never`；`on-failure` 只在 child 非零退出且当前 invocation 的 restart budget 未耗尽时写入 `state=backoff`、等待固定 backoff、重启 child 并继续 heartbeat，预算耗尽时写入 `state=failed` 并返回 `restart_exhausted`，证据为 `tdxquant/provider_transport_replay.py`、`tdxquant/cli.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-daemon-restart-backoff-policy`；该策略不默认启用、不跨 supervisor invocation 持久化 restart budget，不提供指数退避/jitter，不做端口 ownership 推断、进程命令行校验、真实 provider 恢复，也不证明 broker/workflow/write readiness 或 E-06 全部完成。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay daemon status --expected-owner-token --inspect-process-identity` 与 `provider-replay lifecycle-readiness --include-ownership-check` 新增只读 process ownership diagnostics，底层 `build_provider_replay_process_ownership_diagnostics()` 区分 statefile 配置/缺失/无效/过期、config hash、owner token、PID liveness 与可选 process identity match，并让 readiness 只在 statefile 有效且 ownership 证明为 `owned` 时把 `owned_process_identity` 计入 satisfied requirement，证据为 `tdxquant/provider_transport_replay.py`、`tdxquant/cli.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-process-ownership-diagnostics`；该检查不执行 start/stop/kill/restart，不改变默认 stop 行为，不推断端口 ownership，`--inspect-process-identity` 仅是 opt-in 的本地命令行身份诊断，不管理真实 provider 生命周期，也不证明 E-06、broker、workflow 或 write readiness 已完成。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay status --config ... --view detailed/summary` 在 `lifecycle_state_file` 已配置时不再把 lifecycle control 误报为 `unsupported`，而是只读报告 `managed_replay_daemon`、`start_stop_managed=true`、`daemon_managed=true`、`restart_policy=operator_opt_in`、`control_status=operator_opt_in_available`、可用 `start/status/stop/supervise/restart_backoff`、supervisor opt-in 可用，以及 `lifecycle-plan` 对 stale ownership statefile 的非执行 blocked 计划，证据为 `tdxquant/provider_transport_replay.py`、`tdxquant/cli.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-managed-lifecycle-status-summary`；这些字段只从配置和既有 lifecycle metadata 派生，不启动/停止/监督进程，不读取或写入 statefile，不检查进程表或端口 ownership，不代表真实 TongDaXin provider 生命周期、broker/workflow/write readiness 或 E-06 已全部完成。

> E-06 历史补充登记（当时状态为 `[部分实现]`）：`provider-replay lifecycle-readiness --include-statefile-check --include-ownership-check` 新增 managed lifecycle readiness gate，能够从 `operator_opt_in_available` lifecycle status 计入 `lifecycle_controller`、`supervisor_loop`、`operator_opt_in_control`，并且只有在有效未过期 statefile 与 `ownership_status=owned` 同时成立时才返回 `ready=true`，证据为 `tdxquant/cli.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-managed-lifecycle-readiness-gate`；该 readiness 仍为只读判定，不执行 start/stop/supervise/restart，不写 statefile，不推断端口 ownership，不代表 replay HTTP runtime 已启动、真实 TongDaXin provider 生命周期、broker/workflow/write readiness 或 E-06 已全部完成。

> E-06 收口登记（状态提升为 `[已实现]`）：E-06 的 `[已实现]` 仅覆盖 replay fake provider 与其 operator opt-in managed lifecycle，包括 fake HTTP provider、probe/status/config-check summary、statefile ownership lock、daemon start/status/stop、foreground supervisor、restart/backoff、process ownership diagnostics、managed lifecycle status 和 readiness gate，证据为 `tdxquant/provider_transport_replay.py`、`tdxquant/cli.py`、`tests/test_provider_transport_replay.py`、`tests/test_api_cli.py` 与 OpenSpec `provider-replay-daemon-lifecycle-implemented-registry`；该状态不扩展到真实 TongDaXin provider、broker/workflow/read-write readiness、生产交易可用性、端口 ownership 推断或其他节点。

## 4. 非目标与边界

| ID | 功能节点 | 状态 | 证据 | 边界 |
| --- | --- | --- | --- | --- |
| X-01 | WSL 内直接操作 Win32/UIA/HID | `[非目标/边界]` | `README.md` 明确 WSL 侧消费结构化 JSON，交易/数据能力优先走 Windows 原生接口或 bridge | 不在 WSL 内直接驱动桌面控件；相关操作必须在 Windows/bridge 一侧完成。 |
| X-02 | 将 fixture/replay 等同于真实外部可用性 | `[非目标/边界]` | `tdxquant/fixtures/provider/*`、`tdxquant/replay_provider.py`、`tdxquant/provider_transport_replay.py`、`tests/test_replay_provider.py`、`tests/test_provider_transport_replay.py` | fixture/replay/HTTP fake provider 是契约和回归资产；不能作为真实行情、公式、交易、Windows 客户端环境或 live bridge 已就绪的证明。 |
| X-03 | 用独立 ROADMAP 覆盖本文件 | `[非目标/边界]` | 本文件注册规则 | 不再建立与 `FUNCTION_TREE.md` 并列抢真相的 `ROADMAP.md`；待实现项只在本文件分状态登记。 |
| X-04 | 将交易主线包装成全券商生产交易平台 | `[非目标/边界]` | 当前源码主要证据集中在 `PingAnDesktopTraderGateway`、`TdxTradeManager.pingan.*`、平安相关测试与 artifact | 当前是桌面自动化交易辅助和治理层，不承诺多券商、全品种、全异常弹窗覆盖。 |

## 5. 状态变更准入

| 目标状态 | 必要证据 | 不足时的登记方式 |
| --- | --- | --- |
| `[已实现]` | 源码入口 + CLI/manager/task/report/catalog 之一 + 测试或 fixture/contract/runtime preset 证据 + 明确边界 | 缺测试或契约时只能登记为 `[部分实现]`。 |
| `[部分实现]` | 底层能力或局部入口存在，但 contract、transport、fixture、测试、日常入口或运行治理未完全闭合 | 必须在边界中写出未闭合项，并在 E 节保留待实现节点。 |
| `[已设计/待实现]` | 有设计、下一步、OpenSpec、历史计划或明确缺口来源，但没有当前稳定入口 | 必须写明“不代表当前可用”，不得放入“已实现”树枝。 |
| `[非目标/边界]` | 项目定位或安全边界明确排除 | 不得以 backlog 口吻伪装成马上要做的功能。 |

## 6. 关联文档权限

| 文档 | 当前角色 | 权限边界 |
| --- | --- | --- |
| `FUNCTION_TREE.md` | 唯一功能全景图与状态注册表 | 决定功能节点当前状态、证据和边界。 |
| `README.md` | 当前分支定位与常用入口说明 | 可说明入口和使用方式，不替代功能状态注册。 |
| `TdxQuant_handbook.md` | TdxQuant 背景/手册材料 | 可作背景阅读，不代表当前仓库实现状态。 |
| `docs/TdxQuant_Project_Function_Map.md` | 历史/背景功能地图 | 可解释背景，不覆盖本文件状态。 |
| `docs/TdxQuant_Next_Steps.md` | next-step 设计输入 | 可记录后续方向，不作为独立 roadmap 或状态表。 |
| `docs/TdxQuant_MyStocks_Next_Steps.md` | MyStocks 反馈下的设计输入 | 可记录上层反馈，不代表全项目功能状态。 |
| `docs/TdxQuant_API_System_Plan.md` | API/manager 分层设计方案 | 可解释架构分层，不决定功能是否可用。 |
| `docs/TdxQuant_Interface_Coverage_Matrix.md` | 接口覆盖对照矩阵 | 可说明接口覆盖口径，不替代功能状态注册。 |

## 7. 更新日志

| 日期 | 变更 |
| --- | --- |
| 2026-05-29 | B-16/E-09 补充 direct status 与 HTTP/CLI `watch-status --view summary` 的 `status_summary.control_rollup` 登记：只从 reconciled control payload 投影 state/run_id/pid/reason presence，不读取 lock、不证明 PID liveness、ownership、supervisor 或 readiness，不触发 reconnect/backoff/restart/lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-29 | B-16/E-09 补充 direct status 与 HTTP/CLI `watch-status --view summary` 的 `governance.reconnect_rollup` 登记：只从既有 reconnect 诊断投影 failure/backoff presence 信号，不证明 supervisor ownership、真实 backoff 调度或 restart readiness，不触发 reconnect/backoff/restart/lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-29 | B-16/E-09 补充 HTTP/CLI `watch-status --view summary` 的 `governance.decision_summary.reason_source_key_count` 与 `reason_code_key_count` 登记：只从既有 reason summary 投影 unique source/code 计数，不暴露完整 reasons/actions，不触发 reconnect/backoff/restart/lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-29 | B-16/E-09 补充 HTTP/CLI `watch-status --view summary` 的 `governance.decision_summary.primary_reason` 登记：只从既有 reason summary 投影首个治理原因，不暴露完整 reasons/actions，不触发 reconnect/backoff/restart/lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-29 | B-16/E-09 补充 HTTP/CLI `watch-status --view summary` 的 `governance.decision_summary.primary_action_reason_source` 登记：只从既有 action summary 投影首个建议动作原因的 source，不暴露完整 reasons/actions，不触发 reconnect/backoff/restart/lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-29 | B-16/E-09 补充 HTTP/CLI `watch-status --view summary` 的 `governance.decision_summary.primary_action` 与 `primary_action_reason` 登记：只从既有 action summary 投影首个建议动作和原因，不暴露完整 reasons/actions，不触发 reconnect/backoff/restart/lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-29 | B-16/E-09 补充 HTTP/CLI `watch-status --view summary` 的 `governance.evaluation_rollup.primary_evaluated_component` 与 `has_evaluated_component` 登记：只从既有 evaluation summary 投影 evaluated-component compact 字段，不重新计算 staleness、不触发 reconnect/backoff/restart/lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-28 | B-16/E-09 补充 HTTP/CLI `watch-status --view summary` 的 `governance.evaluation_rollup.has_not_evaluated_component`、`component_status_key_count` 与 `evaluated_status_key_count` 登记：只从既有 evaluation summary 投影 compact completeness 字段，不重新计算 staleness、不触发 reconnect/backoff/restart/lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-28 | E-06 主节点提升为 `[已实现]`：收口范围限定为 replay fake provider 与 operator opt-in managed lifecycle；明确不代表真实 TongDaXin provider、broker/workflow/read-write readiness、生产交易可用性或其他节点完成。 |
| 2026-05-28 | E-06 补充 provider-replay managed lifecycle readiness gate 登记：readiness 现在能计入已实现的 lifecycle controller/supervisor/operator opt-in control，并在 statefile 与 ownership 均证明时返回 ready；仍不执行控制、不代表 runtime/provider/write readiness。 |
| 2026-05-28 | E-06 补充 provider-replay managed lifecycle status summary 登记：配置 `lifecycle_state_file` 时 status/summary 不再误报 lifecycle control unsupported，而是只读登记 operator opt-in managed replay daemon controls；不执行控制、不观察进程、不证明真实 provider 或 write readiness。 |
| 2026-05-28 | E-06 补充 provider-replay process ownership diagnostics 登记：daemon status/readiness 新增只读 ownership 证明字段，区分 statefile/hash/token/PID/可选进程身份；不执行控制动作、不推断端口 ownership、不管理真实 provider，也不证明 E-06 完整完成。 |
| 2026-05-28 | E-06 补充 `provider-replay daemon supervise --restart-policy on-failure` 登记：新增显式 opt-in、当前 supervisor invocation 内 bounded restart/backoff；默认不重启，预算耗尽写 `state=failed`，不跨进程持久化预算、不做指数退避/端口 ownership/真实 provider 恢复，也不证明 broker/workflow/write readiness。 |
| 2026-05-28 | E-06 补充 `provider-replay daemon supervise` 登记：新增显式前台 supervisor loop、heartbeat statefile 刷新、child exit/stopping state 记录；不自动 restart/backoff、不推断端口 ownership、不管理真实 provider，也不证明 broker/workflow/write readiness。 |
| 2026-05-28 | E-06 补充 `provider-replay daemon start|status|stop` 登记：新增基于 statefile owner token/config hash/PID 的 one-shot managed replay daemon 控制面；不代表长期 supervisor、restart/backoff、端口 ownership 推断、真实 provider lifecycle、broker/workflow/write readiness 已完成。 |
| 2026-05-28 | E-06 补充 provider-replay lifecycle statefile ownership lock 登记：新增内部锁定原子写 statefile helper 和 ownership 诊断回显；只证明本地 statefile/lock 基础层可用，不执行 start/stop/restart、supervisor、restart/backoff、进程 ownership 或真实 provider lifecycle control。 |
| 2026-05-28 | E-06 补充 `provider-replay lifecycle-readiness` 登记：只读汇总 lifecycle readiness 缺口，默认不读 statefile，opt-in statefile 诊断只满足 `valid_lifecycle_statefile` 一个先决条件；固定 blocked/不可控制，不执行 lifecycle control、不证明 daemon 生命周期或 broker/workflow/write readiness。 |
| 2026-05-26 | E-06 补充 provider-replay `probe_summary.error_sample_visible_count` 登记：只从既有有界 `error_samples` 列表长度派生当前响应可见样本数量；不改变 sample limit/truncated 语义、不暴露完整错误 payload、不证明健康/readiness/endpoint 覆盖，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。 |
| 2026-05-26 | E-06 补充 provider-replay `probe_summary.error_sample_hidden_count` 登记：只从既有 `error_sample_count` 与有界 `error_samples` 长度派生被 sample limit 隐藏的候选数量；不改变 sample limit/truncated 语义、不暴露完整错误 payload、不证明健康/readiness/endpoint 覆盖，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。 |
| 2026-05-26 | E-06 补充 provider-replay `probe_summary.primary_error_sample_error_code` / `primary_error_sample_http_status` 登记：只从既有有界 `error_samples` 列表首个样本派生 error_code/http_status 紧凑诊断；不请求额外 probe、不暴露完整错误 payload、不证明健康/readiness/endpoint 覆盖，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。 |
| 2026-05-27 | E-06 补充 provider-replay `probe_summary.primary_error_sample_reachability` 登记：只从首个错误样本候选派生 reachability 紧凑诊断，`null` 只表示当前没有错误样本候选；不改变 `error_samples` payload 形状或排序、不替代 reachability count maps 或单项 probe 证据、不证明健康/readiness/endpoint 覆盖，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。 |
| 2026-05-26 | E-06 补充 provider-replay `probe_summary.primary_error_sample_probe` / `primary_error_sample_status` 登记：只从既有有界 `error_samples` 列表首个样本派生 probe/status 身份；不请求额外 probe、不暴露完整错误 payload、不证明健康/readiness/endpoint 覆盖，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。 |
| 2026-05-26 | B-16/E-09 补充 subscription `evaluation_summary.primary_evaluated_component` 登记：只从既有 `evaluated_components` 顺序派生首个已评估分量；不改变 staleness 判定、不暴露 raw control/watch payload 或完整 governance arrays，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-26 | E-11 补充 `catalog plan|preview --bundle ... --view summary` 的 `step_source_entry_counts` / `step_source_entry_key_count` 登记：只从 selected bundle steps 的 `source:entry` 组合派生 source-qualified step entry 分布和不同 key 数量；不替代 `selected_step_count`、不暴露 option value 或完整 resolved args、不列出完整 bundle/step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、provider readiness、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog plan|preview --bundle ... --view summary` 的 `step_resolved_arg_key_counts` / `step_source_resolved_arg_key_counts` 及 key-count 登记：只从 selected bundle steps 的 reduced `resolved_args` key 与 `source:key` 组合派生分布和不同 key 数量；不暴露 resolved arg value、raw option value 或完整 resolved args、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、provider readiness、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog plan|preview --bundle ... --view summary` 的 `step_source_name_counts` / `step_source_name_key_count` 登记：只从 selected bundle steps 的 `source:name` 组合派生 source-qualified step name 分布和不同 key 数量；不替代 `selected_step_count`、不暴露 option value 或完整 resolved args、不列出完整 bundle/step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、provider readiness、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog plan|preview --bundle ... --view summary` 的 `step_entry_counts` / `step_entry_key_count` 登记：只从 selected bundle steps 的 `entry` 字段派生 step entry 分布和不同 entry key 数量；不替代 `selected_step_count`、不暴露 option value 或完整 resolved args、不列出完整 bundle/step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、provider readiness、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog plan|preview --bundle ... --view summary` 的 `step_name_counts` / `step_name_key_count` 登记：只从 selected bundle steps 的 `name` 字段派生 step name 分布和不同 name key 数量；不替代 `selected_step_count`、不列出完整 bundle/step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、provider readiness、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog plan|preview --bundle ... --view summary` 的 `step_source_key_count` 登记：只从已投影 selected bundle `step_source_counts` map 派生不同 step source key 数量；不统计 resolved step 总数、不列出完整 bundle/step manifest、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、provider readiness、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-28 | E-11 补充 `catalog plan|preview --bundle ... --view summary` 的 `selected_step_summary.step_name_counts` / `plan_summary.step_name_counts` 登记：只从已选 resolved bundle steps 的既有 step name count map 投影 compact rollup；不新增 step 解析语义、不校验 option 值、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-28 | E-11 补充 `catalog plan|preview --bundle ... --view summary` 的 `selected_step_summary.step_entry_counts` / `plan_summary.step_entry_counts` 登记：只从已选 resolved bundle steps 的既有 step entry count map 投影 compact rollup；不新增 entry 解析语义、不校验 option 值、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-28 | E-11 补充 `catalog plan|preview --bundle ... --view summary` 的 `selected_step_summary.step_source_name_counts` / `plan_summary.step_source_name_counts` 登记：只从已选 resolved bundle steps 的既有 `source:name` count map 投影 compact rollup；不新增 step/source 解析语义、不校验 option 值、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-28 | E-11 补充 `catalog plan|preview --bundle ... --view summary` 的 `selected_step_summary.step_source_entry_counts` / `plan_summary.step_source_entry_counts` 登记：只从已选 resolved bundle steps 的既有 `source:entry` count map 投影 compact rollup；不新增 entry/source 解析语义、不校验 option 值、不执行 catalog entry/task/report/trade/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-06 补充 provider-replay `probe_summary.error_sample_http_status_counts` / `error_sample_http_status_key_count` 登记：只统计进入紧凑错误样本候选集合且带整数 `http_status` 的 HTTP status 分布和不同 key 数量；独立于被截断的 `error_samples` 列表，不替代 requested/healthy/failed HTTP status count maps，不暴露完整 error/probe payload、不证明健康/readiness/HTTP endpoint 覆盖或 failure coverage，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。 |
| 2026-05-27 | E-06 补充 provider-replay `probe_summary.error_sample_reachability_counts` / `error_sample_reachability_key_count` 登记：只统计进入紧凑错误样本候选集合的 reachability 分布和不同 key 数量；独立于被截断的 `error_samples` 列表，不替代 requested/healthy/failed reachability count maps，不暴露完整 error/probe payload、不证明健康/readiness/endpoint 覆盖或 failure coverage，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。 |
| 2026-05-26 | E-06 补充 provider-replay `probe_summary.primary_unhealthy_probe` 登记：只从既有 `unhealthy` 目标列表派生首个 unhealthy probe；`null` 只表示当前没有 unhealthy probe，不替代 `unhealthy_count`、`failed`、`primary_failed_probe` 或单项 probe 证据，不证明服务健康/readiness/endpoint 覆盖或生产 daemon 控制，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。 |
| 2026-05-26 | B-16/E-09 补充 subscription `governance.reason_source_key_count` 登记：只从顶层 `reason_source_counts` map 派生不同 reason source key 数量；不替代 `governance.reason_count`、`reason_source_counts` 或 `reason_summary.source_key_count`，不暴露完整 reasons、不证明健康/readiness/PID liveness/process ownership、升级策略或治理策略，也不触发 reconnect/backoff/restart/lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-26 | B-16/E-09 补充 subscription `action_summary.severity_key_count` / `action_name_key_count` / `reason_source_key_count` / `reason_code_key_count` 登记：只从既有 action summary count map 派生不同 key 数量；不替代 `governance.action_count`、`action_summary.count` 或各 count map，不暴露完整 actions、不代表执行队列/升级策略/自动化治理/健康/readiness/PID liveness/process ownership，也不触发 reconnect/backoff/restart/lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-26 | B-16/E-09 补充 subscription `reason_summary.source_key_count` / `reason_code_key_count` 登记：只从既有 reason summary count map 派生不同 reason source/code key 数量；不替代 `governance.reason_count`、`source_counts` 或 `reason_code_counts`，不暴露完整 reasons、不证明健康/readiness/PID liveness/process ownership 或治理策略，也不触发 reconnect/backoff/restart/lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-26 | B-16/E-09 补充 subscription `evaluation_summary.component_status_key_count` / `evaluated_status_key_count` 登记：只从既有 evaluation status count map 派生不同 status key 数量；不改变 staleness 判断、不替代 component lists/counts 或 status-count maps、不证明健康/readiness/PID liveness/process ownership，也不触发 reconnect/backoff/restart/lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-26 | E-06 补充 provider-replay `probe_summary.error_code_key_count` / `failed_error_code_key_count` / `error_sample_status_key_count` / `error_sample_probe_key_count` 登记：只从既有 error-code/error-sample count map 派生不同 key 数量；不请求额外 probe、不暴露完整 error/probe payload、不证明健康/readiness/failure coverage，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。 |
| 2026-05-26 | E-06 补充 provider-replay `probe_summary.requested_http_status_key_count` / `healthy_http_status_key_count` / `failed_http_status_key_count` 登记：只从既有 probe HTTP status count map 派生不同 HTTP status key 数量；不请求额外 probe、不证明健康/readiness/HTTP endpoint 覆盖，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。 |
| 2026-05-26 | E-06 补充 provider-replay `probe_summary.requested_reachability_key_count` / `healthy_reachability_key_count` / `failed_reachability_key_count` 登记：只从既有 probe reachability count map 派生不同 reachability key 数量；不请求额外 probe、不证明健康/readiness/endpoint 覆盖，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。 |
| 2026-05-26 | E-06 补充 provider-replay `probe_summary.status_key_count` / `requested_status_key_count` / `failed_status_key_count` 登记：只从既有 probe status count map 派生不同 status key 数量；不请求额外 probe、不证明健康/readiness/endpoint 覆盖，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。 |
| 2026-05-26 | E-11 补充 `catalog validate --view summary` 的 selected bundle/task+report step option-key / `source:option_key` key-count 登记：只从已投影 option-key count map 派生不同 key 数量；不暴露 option value、不校验 option 语义、不执行 entry/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog validate --view summary` 的 selected bundle/task+report step `source:name` / `source:entry` key-count 登记：只从已投影 source-qualified step name/entry count map 派生不同 key 数量；不统计 resolved step 总数、不执行 entry/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog validate --view summary` 的 selected bundle/task+report step name/entry key-count 登记：只从已投影 step name/entry count map 派生不同 key 数量；不统计 resolved step 总数、不执行 entry/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog validate --view summary` 的 `entry_source_key_count` / `entry_label_key_count` 登记：只从已投影 `entry_source_counts` / `entry_label_counts` map 派生不同 source/label key 数量；不统计 matched entry 总数、不执行 entry/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog validate --view summary` 的 `submit_once_bundle_step_source_option_key_count` / `pingan_bundle_step_source_option_key_count` 登记：只从已投影 submit-once/PingAn step `source:option_key` count map 派生不同 `source:option_key` 数量；不暴露 option value、不校验 option 语义、不执行 entry/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog validate --view summary` 的 `submit_once_bundle_step_option_key_count` / `pingan_bundle_step_option_key_count` 登记：只从已投影 submit-once/PingAn step option-key count map 派生不同 option-key 数量；不暴露 option value、不校验 option 语义、不执行 entry/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog validate --view summary` 的 `submit_once_bundle_step_source_entry_key_count` / `pingan_bundle_step_source_entry_key_count` 登记：只从已投影 submit-once/PingAn step `source:entry` count map 派生不同 `source:entry` key 数量；不统计 resolved step 总数、不执行 entry/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog validate --view summary` 的 `submit_once_bundle_step_entry_key_count` / `pingan_bundle_step_entry_key_count` 登记：只从已投影 submit-once/PingAn step entry-count map 派生不同 step-entry key 数量；不统计 resolved step 总数、不执行 entry/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog validate --view summary` 的 `submit_once_bundle_step_source_name_key_count` / `pingan_bundle_step_source_name_key_count` 登记：只从已投影 submit-once/PingAn step `source:name` count map 派生不同 `source:name` key 数量；不统计 resolved step 总数、不执行 entry/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog validate --view summary` 的 `submit_once_bundle_step_name_key_count` / `pingan_bundle_step_name_key_count` 登记：只从已投影 submit-once/PingAn step name-count map 派生不同 step-name key 数量；不统计 resolved step 总数、不执行 entry/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog validate --view summary` 的 `submit_once_bundle_step_source_key_count` / `pingan_bundle_step_source_key_count` 登记：只从已投影 submit-once/PingAn step source-count map 派生不同 source key 数量；不统计 resolved step 总数、不执行 entry/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog validate --view summary` 的 `submit_once_bundle_label_key_count` / `pingan_bundle_label_key_count` 登记：只从已投影 submit-once/PingAn label-count map 派生不同 label key 数量；不统计 bundle 总数、不执行 entry/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog validate --view summary` 的 `bundle_label_key_count` / `task_report_bundle_label_key_count` 登记：只从已投影 label-count map 派生不同 label key 数量；不统计 bundle 总数、不执行 entry/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog validate --view summary` 的 `bundle_step_source_key_count` / `task_report_bundle_step_source_key_count` 登记：只从已投影 source-count map 派生不同 source key 数量；不统计 resolved step 总数、不执行 entry/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-06 补充 provider-replay `probe_summary.primary_requested_probe` 登记：只从既有 `requested` 目标列表派生首个已请求 probe；`null` 只表示当前没有 requested probe，不请求额外 probe、不证明健康状态、readiness 或 endpoint 覆盖，也不新增 probe 端点、socket 启动或 daemon 生命周期管理。 |
| 2026-05-26 | B-16/E-09 补充 subscription summary view `runtime.pid_source` 登记：只在 HTTP/CLI summary 已从 `control.pid` 投影 `runtime.pid` 时标注来源为 `control`，不新增 PID fallback、不证明 PID 存活、process ownership、健康/readiness 或生命周期状态，也不触发 reconnect/backoff/restart/lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-26 | B-16/E-09 补充 subscription summary view `runtime.run_id_match` 登记：只在 HTTP/CLI summary 中比较既有 `control.run_id` 与 `watch_status.run_id` 是否相等，不改变 `runtime.run_id` source precedence，不证明 run ownership、健康/readiness、新旧程度或生命周期状态，也不触发 reconnect/backoff/restart/lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-26 | B-16/E-09 补充 subscription summary view `runtime.run_id_source` 登记：只说明 `runtime.run_id` 来自 `watch_status.run_id` 还是 fallback `control.run_id`，不证明 run ownership、健康/readiness 或新旧程度，也不触发 reconnect/backoff/restart/lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-26 | B-16/E-09 补充 subscription summary view `runtime.state_match` 登记：只在 HTTP/CLI summary 中比较既有 `control.state` 与 `watch_status.state` 是否相等，不替代 overall status、staleness 诊断或 governance decision，不证明健康/readiness，也不触发 reconnect/backoff/restart/lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-26 | E-11 补充 `catalog list --view summary` 的 `available_entry_label_count` / `available_bundle_label_count` 登记：只从 summary 已投影的 available label 数组派生 label discovery 数量，不替代 matched entry/bundle count，不执行 entry/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog validate` 的 `task_report_bundle_sample_count` / `submit_once_bundle_sample_count` / `pingan_bundle_sample_count` 登记：只从 summary 已投影的有界 bundle sample 数组派生可见样本数量，不替代完整 bundle count/step count、limit 或 truncated 标志，不执行 entry/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | B-16/E-09 补充 subscription summary view `reason_sample_count` / `action_sample_count` 登记：只从 HTTP/CLI summary 已投影的有界 `reason_samples` / `action_samples` 可见数组派生，不替代完整 `reason_count` / `action_count`，不暴露 raw payload 或完整 governance arrays，也不触发 reconnect/backoff/restart/lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-26 | E-06 补充 provider-replay `probe_summary.primary_not_requested_probe` 登记：只从既有 `not_requested` 目标列表派生首个未请求 probe；`null` 只表示当前没有 not-requested probe，不替代 request coverage 或单项 probe 证据，不证明该目标已被 probe、失败、不可用或需要执行，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。 |
| 2026-05-26 | B-16/E-09 补充 subscription governance `reason_summary.primary_reason_source` 登记：只从既有 `primary_source` 同值派生，并通过 detailed/HTTP summary/CLI summary 投影；不移除 `primary_source`，不暴露完整 reasons，不代表治理策略、执行指令或自动化条件，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-26 | E-06 补充 provider-replay `probe_summary.primary_healthy_probe` 登记：只从既有 `healthy` 目标列表派生首个 healthy probe；`null` 只表示当前没有 healthy probe，不替代 healthy rollup 或单项 probe 证据，不证明服务健康、readiness 或 endpoint 覆盖，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。 |
| 2026-05-26 | E-06 补充 provider-replay `probe_summary.healthy_reachability_counts` 登记：只统计已请求且 `status=healthy` probe 的 `reachable` 分桶并排除 failed/`not_requested`；不替代 requested reachability、healthy HTTP status 或单项 probe 证据，不证明服务健康、HTTP 覆盖或 endpoint 覆盖，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。 |
| 2026-05-26 | B-16/E-09 补充 subscription governance `action_summary.primary_severity` 登记：只从首个 advisory action 的 severity 派生并通过 detailed/HTTP summary/CLI summary 投影；与兼容字段 `severity` 同口径，不暴露完整 actions，不代表执行等级、自动升级策略或治理策略，也不触发 reconnect、backoff、restart、lifecycle、HTTP、SSE 或 event-stream 行为。 |
| 2026-05-26 | E-06 补充 provider-replay `probe_summary.failed_reachability_counts` 登记：只统计已请求且非 `healthy` probe 的 `reachable` 分桶并排除 `healthy`/`not_requested`；不替代 requested reachability、failed status 或单项 probe 证据，不证明健康状态、失败覆盖或 endpoint 覆盖，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。 |
| 2026-05-26 | E-06 补充 provider-replay `probe_summary.primary_failed_probe` 登记：只从既有 `failed` 目标列表派生首个 failed probe；`null` 只表示当前没有 failed probe，不替代 failed/error rollup、不证明健康状态或 endpoint 覆盖，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。 |
| 2026-05-26 | E-06 补充 provider-replay `probe_summary.request_coverage_status` 登记：只从既有 `requested_count/total_count` 派生 `none` / `partial` / `complete` 请求覆盖状态；不请求额外 probe、不证明健康状态或 endpoint 覆盖，也不新增 probe 端点、socket 启动、provider mutation、调度、restart/backoff 或 daemon 生命周期管理。 |
| 2026-05-26 | E-11 补充 `catalog validate` 的 `submit_once_bundle_step_source_entry_counts` 与 `pingan_bundle_step_source_entry_counts` 登记：只统计 selected resolved submit-once/PingAn bundle step `source:entry` 分布；不列出完整 step manifest、不执行 entry/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog validate` 的 `submit_once_bundle_step_count` 与 `pingan_bundle_step_count` 登记：只统计 selected resolved submit-once/PingAn bundle step 总数；不列出完整 step manifest、不执行 entry/bundle step，也不代表 workflow builder、broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog validate` 的 `submit_once_bundle_step_source_option_key_counts` 与 `pingan_bundle_step_source_option_key_counts` 登记：只统计 selected resolved submit-once/PingAn bundle step `source:option_key` 分布；空 map 仅表示当前选集无 source-qualified option key，不暴露 option value、不执行 entry/bundle step，也不代表 broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog validate` 的 `submit_once_bundle_step_option_key_counts` 与 `pingan_bundle_step_option_key_counts` 登记：只统计 selected resolved submit-once/PingAn bundle step option key 分布；空 map 仅表示当前选集无 option key，不暴露 option value、不执行 entry/bundle step，也不代表 broker readiness、交易安全证明或执行覆盖。 |
| 2026-05-26 | E-11 补充 `catalog validate` 的 `task_report_bundle_step_source_option_key_counts` 登记：只统计 selected resolved task+report bundle step 的 `source:option_key` 分布；不暴露 option value、不执行 entry/bundle step，也不代表 workflow builder、执行覆盖或 readiness 证明。 |
| 2026-05-26 | E-11 补充 `catalog validate` 的 `bundle_step_source_option_key_counts` 登记：只统计 selected resolved bundle step 的 `source:option_key` 分布；不暴露 option value、不执行 entry/bundle step，也不代表 workflow builder、执行覆盖或 readiness 证明。 |
| 2026-05-26 | B-16/E-09 补充 subscription `evaluation_summary.primary_fresh_component` 登记：只从既有 `fresh_components` 顺序派生首个 fresh 分量并通过 detailed/HTTP/CLI summary 投影；不暴露 raw payload 或完整 governance arrays，也不触发 reconnect/backoff/restart/lifecycle。 |
| 2026-05-25 | B-16/E-09 补充 subscription `evaluation_summary.primary_not_evaluated_component` 登记：只从既有 `not_evaluated_components` 顺序派生首个未评估分量并通过 detailed/HTTP/CLI summary 投影；不暴露 raw payload 或完整 governance arrays，也不触发 reconnect/backoff/restart/lifecycle。 |
| 2026-05-25 | B-16/E-09 补充 subscription `evaluation_summary.primary_stale_component` 登记：只从既有 `stale_components` 顺序派生首个 stale 分量并通过 detailed/HTTP/CLI summary 投影；不暴露 raw payload 或完整 governance arrays，也不触发 reconnect/backoff/restart/lifecycle。 |
| 2026-05-25 | E-06 补充 provider-replay `probe_summary.error_sample_probe_counts` 登记：只统计进入紧凑错误样本候选集合的 probe key 分布，并独立于被截断的 `error_samples` 列表；不暴露完整 probe payload、不证明失败覆盖或健康状态，也不新增 probe 端点、socket 启动或 daemon 生命周期管理。 |
| 2026-05-25 | E-11 补充 `catalog validate` 的 `bundle_step_source_entry_counts` 登记：只统计 selected resolved bundle step 的 `source:entry` 分布；不列出完整 bundle/step manifest、不执行 entry/bundle step，也不代表 workflow builder、执行覆盖或 readiness 证明。 |
| 2026-05-25 | E-11 补充 `catalog validate` 的 `task_report_bundle_step_source_entry_counts` 登记：只统计 selected resolved task+report bundle step 的 `source:entry` 分布；不列出完整 step manifest、不执行 entry/bundle step，也不代表 workflow builder、执行覆盖或 readiness 证明。 |
| 2026-05-25 | E-06 补充 provider-replay `probe_summary.error_sample_status_counts` 登记：只统计错误样本候选集合的 probe status 分布；不替代 `failed_status_counts`、不暴露完整 probe payload，也不新增 probe 端点、socket 启动或 daemon 生命周期管理。 |
| 2026-05-25 | E-06 补充 provider-replay `probe_summary.error_sample_count` 登记：只统计同一错误样本候选集合总数；不暴露完整 probe payload、不证明失败覆盖，也不新增 probe 端点、socket 启动或 daemon 生命周期管理。 |
| 2026-05-25 | B-16/E-09 补充 subscription governance `action_summary.primary_reason_source` 登记：只从第一条 advisory action reason 解析 source 前缀，并通过详细与 summary action rollup 投影；不暴露完整 actions，也不触发 reconnect/backoff/restart/lifecycle。 |
| 2026-05-25 | E-06 补充 provider-replay `probe_summary.failed_http_status_counts` 登记：只统计已请求且非 `healthy` probe 的整数 `http_status` 分布；不新增 probe 端点、socket 启动、失败覆盖证明或 daemon 生命周期管理。 |
| 2026-05-25 | E-06 补充 provider-replay `probe_summary.healthy_http_status_counts` 登记：只统计已请求且 `status=healthy` probe 的整数 `http_status` 分布；不新增 probe 端点、socket 启动、HTTP 覆盖证明或 daemon 生命周期管理。 |
| 2026-05-25 | E-11 补充 `catalog validate` 的 `submit_once_bundle_step_entry_counts` 与 `pingan_bundle_step_entry_counts` 登记：只统计 selected resolved submit-once/PingAn bundle 子集的 resolved step entry 分布；不执行 entry/bundle step，也不代表完整 step manifest、执行链路、broker readiness 或交易安全证明。 |
| 2026-05-25 | E-11 补充 `catalog validate` 的 `submit_once_bundle_step_source_name_counts` 与 `pingan_bundle_step_source_name_counts` 登记：只统计 selected resolved submit-once/PingAn bundle 子集的 resolved step `source:name` 分布；不执行 entry/bundle step，也不代表执行链路、broker readiness 或交易安全证明。 |
| 2026-05-25 | E-11 补充 `catalog validate` 的 `submit_once_bundle_step_name_counts` 与 `pingan_bundle_step_name_counts` 登记：只统计 selected resolved submit-once/PingAn bundle 子集的 resolved step name 分布；不执行 entry/bundle step，也不代表执行链路、broker readiness 或交易安全证明。 |
| 2026-05-25 | E-11 补充 `catalog validate` 的 `submit_once_bundle_step_source_counts` 与 `pingan_bundle_step_source_counts` 登记：只统计 selected resolved submit-once/PingAn bundle 子集的 resolved step source 分布；不执行 entry/bundle step，也不代表执行链路、broker readiness 或交易安全证明。 |
| 2026-05-25 | E-11 补充 `catalog validate` 的 `submit_once_bundle_label_counts` 与 `pingan_bundle_label_counts` 登记：只统计 selected resolved submit-once/PingAn bundle 子集的 bundle labels；不执行 entry/bundle step，也不代表执行覆盖、broker readiness 或交易安全证明。 |
| 2026-05-25 | E-06 补充 provider-replay `probe_summary.failed_error_code_counts` 登记：只统计已请求且非 `healthy` probe 的字符串 `error_code` 分布并排除 `healthy`/`not_requested`；不新增 probe 端点、socket 启动或 daemon 生命周期管理。 |
| 2026-05-25 | B-16/E-09 补充 subscription governance `action_summary.reason_code_counts` 登记：只读统计 advisory action reason 完整字符串分布，并通过 CLI/HTTP summary view 投影；不暴露完整 actions，也不触发 reconnect/backoff/restart/lifecycle。 |
| 2026-05-25 | B-16/E-09 补充 subscription governance `action_summary.reason_source_counts` 登记：只读统计 advisory action reason 来源前缀，并通过 CLI/HTTP summary view 投影；不暴露完整 actions，也不触发 reconnect/backoff/restart/lifecycle。 |
| 2026-05-25 | E-06 补充 provider-replay `probe_summary.requested_http_status_counts` 登记：只统计已请求 probe 的整数 `http_status` 分布并排除 `not_requested`、缺失状态和非整数状态；不新增 probe 端点、socket 启动或 daemon 生命周期管理。 |
| 2026-05-25 | E-11 补充 `catalog validate` 的 `entry_source_counts` 登记：只统计 selected resolved catalog entry 的 source 分布；不执行 entry，也不代表执行覆盖、可用性证明或 readiness。 |
| 2026-05-25 | E-06 补充 provider-replay `probe_summary.requested_reachability_counts` 登记：只统计已请求 probe 的 `reachable` 分桶并排除 `not_requested`；不新增 probe 端点、socket 启动或 daemon 生命周期管理。 |
| 2026-05-25 | B-16/E-09 补充 subscription `evaluation_summary` component list/count 字段登记：逐名登记 `evaluated_components`、`stale_components`、`not_evaluated_components` 及对应 count；仅为既有只读摘要契约，不触发 reconnect/backoff/restart/lifecycle。 |
| 2026-05-25 | E-11 补充 `catalog validate` 的 `entry_label_counts` 登记：只统计 selected resolved catalog entry 的 label 分布；不执行 entry，也不代表执行覆盖、可用性证明或 readiness。 |
| 2026-05-25 | E-11 补充 `catalog validate` 的 `bundle_step_option_key_counts` 与 `task_report_bundle_step_option_key_counts` 登记：只统计 selected resolved bundle step 的 options key；不验证 option value、不执行 step，也不代表任意 workflow builder。 |
| 2026-05-25 | E-06 补充 provider-replay `probe_summary.failed_status_counts` 登记：只统计已请求且非 `healthy` probe 的状态分布并排除 `healthy`/`not_requested`；不新增 probe 端点、socket 启动或 daemon 生命周期管理。 |
| 2026-05-25 | B-16/E-09 补充 subscription `evaluation_summary.evaluated_status_counts` 登记：只读统计已显式评估 heartbeat/watermark/reconnect 分量的 fresh/stale 状态分布并排除 `not_evaluated`；不代表健康保证、自动恢复条件或生命周期控制。 |
| 2026-05-25 | E-06 补充 provider-replay `probe_summary.requested_status_counts` 登记：只统计已请求 probe 的状态分布并排除 `not_requested`；不新增 probe 端点、socket 启动或 daemon 生命周期管理。 |
| 2026-05-25 | E-11 补充 `catalog validate` 的 `task_report_bundle_step_entry_counts` 登记：只对包含 task+report 的 selected resolved bundle 子集做 step entry 聚合计数，并通过 summary view 投影；不代表完整 entry 清单、执行证据或执行链路。 |
| 2026-05-25 | E-11 补充 `catalog validate` 的 `task_report_bundle_step_source_name_counts` 登记：只对包含 task+report 的 selected resolved bundle 子集做 step `source:name` 聚合计数，并通过 summary view 投影；不代表完整步骤明细、执行证据或执行链路。 |
| 2026-05-25 | B-16/E-09 补充 subscription governance `reason_summary.reason_code_counts` 登记：只读统计完整 advisory reason 字符串分布，并通过 HTTP/CLI summary view 投影；不暴露完整 reasons，也不触发 reconnect/backoff/restart/lifecycle。 |
| 2026-05-25 | E-11 补充 `catalog validate` 的 `task_report_bundle_step_name_counts` 登记：只对包含 task+report 的 selected resolved bundle 子集做 step name 聚合计数，并通过 summary view 投影；不代表完整步骤明细或执行链路。 |
| 2026-05-25 | E-11 补充 `catalog validate` 的 `bundle_step_source_name_counts` 登记：只对 selected resolved bundle 的 step `source:name` 做聚合计数，并通过 summary view 投影；不代表完整 bundle/step 明细、执行证据或任意 workflow builder。 |
| 2026-05-25 | B-16/E-09 补充 subscription `evaluation_summary.component_status_counts` 登记：只读统计 heartbeat/watermark/reconnect 的 fresh/stale/not_evaluated 分量数量；不代表健康保证、自动恢复条件或生命周期控制。 |
| 2026-05-25 | E-06 补充 provider-replay `probe_summary.failed` 登记：只从既有只读 probe 对象派生 failed 目标列表并与 `failed_count` 对齐；不新增 probe 端点、socket 启动或 daemon 生命周期管理。 |
| 2026-05-25 | B-16/E-09 补充 subscription governance `action_count` 登记：详细 status summary 与 HTTP/CLI summary view 只读投影 advisory action 数量；不暴露完整 actions，也不触发 reconnect/backoff/restart/lifecycle。 |
| 2026-05-25 | E-11 补充 `catalog validate` 的 `bundle_step_entry_counts` 登记：只对 selected resolved bundle 的 step entry 做聚合计数，并通过 summary view 投影；不代表完整 bundle/step/entry 明细、执行证据或任意 workflow builder。 |
| 2026-05-25 | E-11 补充 `catalog validate` 的 `bundle_step_name_counts` 登记：只对 selected resolved bundle 的 step name 做聚合计数，并通过 summary view 投影；不代表完整 bundle/step 明细、执行证据或任意 workflow builder。 |
| 2026-05-25 | E-06 补充 provider-replay `probe_summary.error_samples` 登记：只从既有只读 probe 对象派生最多 3 个紧凑错误样本及截断标志，不暴露完整 probe payload；不代表新增 probe 端点、socket 启动或 daemon 生命周期管理。 |
| 2026-05-25 | E-06 补充 provider-replay `probe_summary.error_code_counts` 登记：只统计既有只读 probe 对象中的错误码，帮助区分 fake provider 探测失败类别；不代表新增 probe 端点、socket 启动或 daemon 生命周期管理。 |
| 2026-05-25 | B-16/E-09 补充 subscription long-run governance `reason_summary` 登记：仅为 `governance.reasons` 派生的只读 compact rollup，并通过 CLI/HTTP summary view 投影；不代表新增 reconnect/backoff/restart/lifecycle 控制。 |
| 2026-05-14 | 新增/重建根目录 `FUNCTION_TREE.md` 为“功能全景图 + 状态注册表”单一事实源；归并 `docs/TdxQuant_Project_Function_Map.md`、`README.md`、runtime 配置、源码与测试证据；明确不再单独创建 `ROADMAP.md`；补充关联文档权限表，并让 README、手册、next-step、功能地图、API 方案、接口覆盖矩阵等文档显式服从本文件；同步 `subscription HTTP/SSE 推送语义` 为 `[部分实现]`，登记 read-only bridge event-stream v1 的证据与边界。 |
